# Project ITDA(잇다): AI 기반 수어 번역 및 교육 플랫폼
## [개발 진행 보고서] — 2026.05.31 최종 업데이트

본 보고서는 프로젝트 ITDA의 전체 개발 이력과 현재 시스템 상태, 향후 추진 계획을 세세하게 정리합니다.

---

## 1. 프로젝트 개요 (Project Overview)
- **프로젝트 명**: ITDA (잇다)
- **핵심 목표**: 청각장애인과 비장애인의 원활한 소통을 위해 AI(SLM/RAG/Vision) 기술을 활용한 실시간 수어 번역 및 교육용 아바타 플랫폼 구축.
- **최상위 설계 원칙**: **"수화 캐릭터가 수화를 정확히 표현하는 것."** 단순한 동작 흉내를 넘어, 해부학적 정확도와 표준 수형 규격(KSL)을 준수하는 고정밀 애니메이션 구현.

---

## 2. 전체 개발 진행률 (Overall Progress)

4/28 보고서 시점 92% → **5/31 현재 97%**.

| 단계 | 개발 항목 | 4/28 상태 | 5/31 상태 |
| :--- | :--- | :---: | :---: |
| **1단계** | RAG 기반 수어 지식 베이스 | 완료 | 완료 |
| **2단계** | MediaPipe 비전 엔진 & 리타겟팅 | 완료 | 완료 |
| **3단계** | SLM-RAG 하이브리드 번역 엔진 | 완료 | **완료** (문맥 인식 토크나이저 탑재) |
| **4단계** | Three.js 3D 캐릭터 대시보드 | 완료 | 완료 |
| **5단계** | 전문 수어 애니메이션 엔진 | 최적화 | **완료** (영상 전환 시스템 + Hermite 전환) |
| **6단계** | 실시간 음성 인식(STT) 통합 | 완료 | 완료 |
| **7-8단계** | 교육 모드 및 대시보드 | 고도화 | 고도화 |
| **9단계** | 멀티 레이어 렌더링 | 완료 | 완료 |
| **10단계** | 수어 영상 DB 대규모 확장 | 미착수 | **완료** (80개→1,113개) |
| **11단계** | 한국어 형태소 처리 엔진 | 미착수 | **완료** (활용어미/조사/불규칙 처리) |

---

## 3. 핵심 기술 개발 상세

### 3.1. 수어 단어 간 부드러운 전환 시스템 (2026.05.27 구현)

#### 3.1.1. 키프레임 모션 전환 — Hermite 스플라인

**문제**: 수어 문장 재생 시 단어 사이에 250ms 데드 갭 + 순간 이동 현상.

**해결**: 단어 종료 시 다음 단어의 시작 포즈를 미리 확인하여, Hermite 스플라인 기반 전환 프레임을 자동 생성.

| 추가 함수 | 역할 |
|-----------|------|
| `_poseDistance(poseA, poseB)` | 두 포즈 간 평균 각도 거리(라디안) 계산 |
| `_estimateAngularVelocity(keyframes, type)` | 모션 경계에서 각속도 추정 (exit/entry) |
| `_generateTransitionKeyframes(...)` | Hermite 스플라인 기반 전환 keyframe 배열 생성 (smoothstep + 모멘텀 접선) |
| `_playTransitionFrames(avatar, kfs, dur)` | rAF 기반 전환 micro-animation 재생 |
| `_peekNextMotion()` | 큐에서 다음 단어 모션 데이터 미리보기 |

**동작 흐름**:
```
단어 A 재생 완료
  → _peekNextMotion()으로 단어 B 모션 확인
  → _poseDistance()로 포즈 거리 측정
  → 거리 기반 전환 시간 결정 (80ms ~ 350ms 적응형)
  → _generateTransitionKeyframes()로 Hermite 전환 프레임 생성
  → _playTransitionFrames()로 부드러운 micro-animation 재생
  → 단어 B 재생 시작
```

#### 3.1.2. 적응형 크로스페이드

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 크로스페이드 | 고정 300ms | 포즈 거리 기반 100~500ms 적응형 |
| 단어 간 갭 | 250ms 데드 갭 | 0ms (전환 애니메이션이 대체) |

#### 3.1.3. 영상(Video) 전환 시스템 개선

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 크로스페이드 시간 | 150ms | 350ms |
| trimEnd (차렷 구간 자르기) | 0.5s / 0s | 1.8s |
| 영상 종료 | 800ms 검정 페이드아웃 | 즉시 전환 (검정 화면 없음) |
| 영상 시작 | 즉시 (검정 프레임 노출) | `playing` 이벤트 시 표시 (첫 프레임 렌더 후) |
| 이미 재생 중인 영상 | src 재설정 → 리로드 → 깜빡임 | `alreadyPlaying` 감지 → 스킵 |
| 재생 속도 | 1.0x (기본) | 0.9x (자연스러운 속도) |
| 다음 영상 시작점 | 0.3초 | 1.0초 (차렷 구간 건너뛰기) |

**파일**: `frontend/js/motion_loader_v3.js`

---

### 3.2. 문맥 인식 토크나이저 (2026.05.28 구현)

#### 3.2.1. 활용어미·조사 스트리핑 엔진

**문제**: "사랑합니다" → "사랑" + unknown("합니다"). 한국 수어는 문법 어미를 표현하지 않으므로 어간만 추출하면 됨.

**해결 — 3단계 fallback 파이프라인**:
```
입력 "사랑합니다"
  → ① 사전에서 통째 검색 → 없음
  → ② _strip_and_lookup(): suffix "합니다" 제거 → "사랑" → 사전 매칭
  → 결과: {word: "사랑합니다", canonical: "사랑"}
```

**활용어미 패턴 (60+ 종)**:
- 격식체: ~합니다, ~습니다, ~습니까, ~합니까
- 비격식: ~해요, ~어요, ~아요
- 반말: ~해, ~어, ~아
- 과거: ~했다, ~었다, ~았다
- 종결: ~구만, ~구나, ~군요, ~네요, ~죠, ~잖아, ~거든, ~더라고
- 인용: ~다고, ~라고, ~다며, ~다면서
- 연결: ~면서, ~니까, ~지만, ~어서, ~려고
- 관형/부사: ~게, ~운, ~은, ~ㄴ, ~을

**조사 패턴 (22종)**:
- ~은/는, ~이/가, ~을/를, ~에서, ~에게, ~까지, ~부터, ~조차, ~의 등

#### 3.2.2. 불규칙 활용 처리

**이중 안전망 구조**:

1. **`_IRREGULAR_STEMS` 매핑 테이블** (motion_tokenizer.py, 30+개)
   - suffix 스트리핑 후 어간이 사전에 없으면 불규칙 매핑으로 원형 복원
   - ㅂ불규칙: "반가"→"반갑다", "고마"→"고맙다", "아름다"→"아름답다" 등
   - ㄷ불규칙: "걸"→"걷다", "들"→"듣다"
   - 르불규칙: "몰"→"모르다", "빨"→"빠르다"
   - ㅎ불규칙: "파라"→"파랗다", "까마"→"까맣다"

2. **`SYNONYMS` 테이블 확장** (supabase_service.py, 150+개)
   - 각 불규칙 용언에 기본형/반말/높임/과거/관형/부사형 전부 등록
   - 예: 반가워, 반가워요, 반가웠어, 반가웠습니다, 반가운, 반갑게, 반갑다고, 반갑다며 → 모두 "반갑다"

#### 3.2.3. 통합 어간 탐색 함수 `_lookup_stem()`

어간을 4단계로 탐색:
```
① 직접 매칭 ("사랑" → 사전에 있음)
② +다 매칭 ("먹" → "먹다" 사전에 있음)
③ +하다 매칭 ("감사" → "감사하다" 확인)
④ 불규칙 매핑 ("반가" → _IRREGULAR_STEMS → "반갑다")
```

#### 3.2.4. 토크나이저 처리 검증

| 입력 | 처리 경로 | 결과 |
|------|----------|------|
| 사랑합니다 | suffix "합니다" → "사랑" | **사랑** |
| 감사합니다 | suffix "합니다" → "감사" | **감사** |
| 반갑습니다 | suffix "습니다" → "반갑" → "반갑다" | **반갑다** |
| 반가워요 | SYNONYMS 직접 | **반갑다** |
| 반갑구만 | suffix "구만" → "반갑" → "반갑다" | **반갑다** |
| 추워요 | SYNONYMS 직접 | **춥다** |
| 어려워 | SYNONYMS 직접 | **어렵다** |
| 학교에서 | 조사 "에서" → "학교" | **학교** |
| 좋구만 | suffix "구만" → "좋" → "좋다" | **좋다** |
| 피곤하구만 | suffix "구만" → "피곤하" → "피곤하다" | **피곤하다** |

**파일**: `api/services/motion_tokenizer.py`, `api/services/supabase_service.py`

---

### 3.3. 수어 영상 DB 대규모 확장 (2026.05.28~05.31 실행)

#### 3.3.1. 영상 출처 분석

| 항목 | 결과 |
|------|------|
| 원본 출처 | 국립국어원 한국수어사전 (sldict.korean.go.kr) |
| KCISA API 경유 | 문화데이터광장 API로 URL 수집 |
| 해상도 | 700x466 (전체 동일) |
| fps | 59.94fps (744개), 29.97fps (230개) 등 |
| 촬영자 | 동일 촬영 환경 (수어사전 표준 촬영) |

#### 3.3.2. 배경 제거 파이프라인

```
sldict URL (sign_source_video 테이블)
  → mp4 다운로드
  → rembg (U2-Net) 프레임별 배경 제거
  → FFmpeg VP9 인코딩 → 투명 webm (yuva420p)
  → Supabase Storage(sign_videos) 업로드
  → sign_source_video.video_url 업데이트
```

#### 3.3.3. 배치 처리 결과

| 항목 | 수치 |
|------|------|
| 처리 전 (영상 사용 가능) | 80개 |
| sldict URL 보유 (DB) | 1,115개 |
| 배경 제거 처리 성공 | **1,113개** |
| 처리 실패 | 2개 ("배송", "길이" — sldict 서버 오류) |
| 처리 시간 | 약 68시간 (CPU 기준) |
| webm 평균 크기 | 613KB |
| 파일명 규칙 | `sign_{id}_transparent.webm` |

#### 3.3.4. 한글 파일명 문제 해결

Supabase Storage가 한글 키를 거부(`InvalidKey`)하여 파일명을 `sign_{id}_transparent.webm` 형태의 영문/숫자 기반으로 변경.

**파일**: `api/tools/batch_sldict_bg.py` (신규 생성)

---

### 3.4. 아바타 관절 데이터 정밀화 (2026.05.26)

- **REST_DIRECTIONS GLB 실측**: `pygltflib`로 `sonyr.glb` 모델의 실제 bone world position을 추출, 37개 본 전체의 rest direction을 GLB 실측값으로 교정
- **손목 보정 일원화**: 모션 로드 시점에서 `_bakeRetargetCorrection()`으로 Hand 본 쿼터니언에 보정을 1회 미리 적용
- **Supabase 모션 손가락 자동 보완**: 손가락 본이 누락된 모션에 기본 수형('5지') 자동 적용

### 3.5. 영상 우선 모드 전환 (2026.05.26)

- Supabase에서 `video_url`이 있으면 모션(keyframes)보다 **영상 우선 재생**
- 더블 버퍼링 영상 재생 시스템 구축 (backVid 프리로드 → 크로스페이드)
- 영상 배경 제거 파이프라인 (rembg U2-Net) 구축

---

## 4. 현재 시스템 상태 (2026.05.31 기준)

### 4.1. Supabase DB 현황

| 테이블 | 수량 | 설명 |
|--------|------|------|
| sign_lemma | **1,147개** | 등록된 수어 단어 (표제어) |
| sign_motion | **1,364개** | 키프레임 모션 데이터 |
| sign_source_video | **1,265개** | 영상 메타데이터 |
| - transparent webm | **997개** | 배경 제거 완료 (사용 가능) |
| - 미처리 | 2개 | sldict 서버 오류 |
| 로컬 인덱스 (index.json) | **5,987개** | 프론트엔드 단어 목록 |

### 4.2. Supabase Storage 현황

| 항목 | 수치 |
|------|------|
| 버킷 | sign_videos |
| webm 파일 | 37개 (기존 업로드분) |
| mp4 파일 | 63개 |
| 총 용량 | 91.7MB |

> **참고**: 신규 처리된 1,113개 webm은 DB URL이 업데이트되었으나 Storage 파일 수와 불일치. 실제 Storage 업로드 상태 검증 필요.

### 4.3. 시스템 아키텍처

```
┌─ Frontend (port 3000) ──────────────────────────────────────────┐
│  index.html                                                      │
│  ├─ motion_loader_v3.js : V3 쿼터니언 엔진 + Hermite 전환        │
│  │   ├─ _playVideoOnly()     : 더블 버퍼링 영상 재생 (0.9x 속도) │
│  │   ├─ _playCore()          : 키프레임 애니메이션                │
│  │   ├─ _generateTransitionKeyframes() : Hermite 전환 프레임     │
│  │   └─ playSequence()       : 문장 단위 큐 재생                 │
│  ├─ translator.js       : 텍스트/STT → 토크나이저 → 재생 오케스트레이션 │
│  ├─ vision.js           : MediaPipe 비전 + 지터 제거 필터         │
│  └─ avatar.js           : Three.js 3D 렌더링 (현재 비활성)       │
└──────────────────────────────────────────────────────────────────┘
                         │
                  WebSocket / REST (8000)
                         │
┌─ Backend (port 8000) ────────────────────────────────────────────┐
│  api/main.py                                                      │
│  ├─ services/motion_tokenizer.py : 문맥 인식 토크나이저            │
│  │   ├─ 최장일치 greedy                                           │
│  │   ├─ 활용어미 스트리핑 (60+ 패턴)                              │
│  │   ├─ 조사 스트리핑 (22 패턴)                                   │
│  │   └─ 불규칙 활용 복원 (_IRREGULAR_STEMS 30+개)                 │
│  ├─ services/supabase_service.py : Supabase 연동 + SYNONYMS 150+ │
│  ├─ services/rag_engine.py       : FAISS 기반 의미 검색           │
│  ├─ services/slm_agent.py        : Ollama qwen3:4b 에이전트      │
│  ├─ routers/sign_language.py     : 수어 API 엔드포인트            │
│  ├─ routers/ws_vision.py         : WebSocket 비전 라우터          │
│  └─ routers/stt.py               : 음성 인식 라우터               │
└──────────────────────────────────────────────────────────────────┘
                         │
┌─ Supabase (Cloud) ───────────────────────────────────────────────┐
│  PostgreSQL: sign_lemma / sign_motion / sign_source_video /       │
│              sign_alias / sign_language_data(레거시)               │
│  Storage:    sign_videos 버킷 (투명 webm + 원본 mp4)             │
└──────────────────────────────────────────────────────────────────┘
                         │
┌─ ITDA_DB (Desktop 도구) ─────────────────────────────────────────┐
│  c:\Users\ComHolic\Desktop\ITDA_DB                                │
│  ├─ migrations/  : 스키마 생성/마이그레이션                        │
│  ├─ src/         : 영상→모션 추출 파이프라인                       │
│  └─ check_db_status.py : DB 상태 모니터                           │
└──────────────────────────────────────────────────────────────────┘
```

### 4.4. 데이터 파이프라인

```
[텍스트 입력]
  → motion_tokenizer.tokenize("감사합니다")
  → 최장일치 시도 → 실패 → 활용어미 "합니다" 제거 → "감사" 사전 매칭
  → [{word: "감사합니다", source: "local", canonical: "감사"}]
  → 프론트엔드 playSequence(["감사"])
  → Supabase에서 "감사" 영상 URL 조회
  → 투명 webm 더블 버퍼링 재생 (0.9x 속도, 350ms 크로스페이드)
```

---

## 5. 코드 품질 점검 결과 (2026.05.31)

### 5.1. 수정 완료된 버그

| 파일 | 버그 | 수정 내용 |
|------|------|----------|
| `motion_tokenizer.py` | `"고마"→"감사"` 잘못된 불규칙 매핑 | `"고마"→"고맙다"`로 수정 |
| `motion_tokenizer.py` | `"무서"→"두려움"` 명사로 매핑 | `"무서"→"무섭다"`로 수정 |
| `supabase_service.py` | "인사" 키 중복 (덮어쓰기 발생) | 중복 제거 |
| `supabase_service.py` | "목소리" 키 중복 | 중복 제거 |
| `motion_loader_v3.js` | `_playTransitionFrames` 에러 시 finalize 미호출 | `.catch()` 추가 |
| `motion_loader_v3.js` | `playbackRate` 변수 미선언 (ReferenceError) | `비디오요소.playbackRate = 0.9`로 수정 |

### 5.2. 미수정 (향후 개선 권장)

| 우선도 | 항목 | 설명 |
|--------|------|------|
| HIGH | `translator.js` 중복 `_playVideoOnly` | V3 엔진 버전으로 통합 필요 |
| HIGH | Storage 실제 파일 검증 | DB에 997개 URL 등록, Storage 파일 수 불일치 확인 필요 |
| MEDIUM | `sign_language.py` 하드코딩 경로 | Windows 경로 → 환경변수 |
| MEDIUM | `playbackRate` 하드코딩 (0.9) | 설정 가능하도록 변수화 |
| LOW | `vision.js` blendBuffer 메모리 누수 | 장시간 세션 시 미사용 키 누적 |
| LOW | `index.html` education.js 버전 문자열 누락 | 캐시 무효화 안 됨 |

---

## 6. 수정된 파일 목록 (전체)

| 파일 | 변경 내용 | 시기 |
|------|----------|------|
| `frontend/js/motion_loader_v3.js` | Hermite 전환 함수 5개 추가, 영상 전환 개선, 적응형 크로스페이드, playbackRate 0.9x | 05.27~29 |
| `api/services/motion_tokenizer.py` | 활용어미 60+종, 조사 22종, 불규칙 어간 30+개, _strip_and_lookup, _lookup_stem 추가 | 05.28 |
| `api/services/supabase_service.py` | SYNONYMS 83→150+ 확장, ㅂ/ㄷ/르/ㅎ 불규칙 활용형, 중복 키 정리 | 05.28~31 |
| `api/tools/batch_sldict_bg.py` | 신규 — sldict URL 배경 제거 배치 처리 스크립트 | 05.28 |
| `api/tools/mediapipe_retarget.py` | REST_DIRECTIONS GLB 실측값 교정 | 05.26 |

---

## 7. 향후 추진 계획

### 7.1. 즉시 실행 (1주 이내)

#### 7.1.1. Storage 파일 정합성 검증 및 복구
- DB에 등록된 997개 transparent webm URL과 실제 Storage 파일 대조
- 누락된 파일 재업로드 또는 URL 수정
- 예상 소요: 1일

#### 7.1.2. `translator.js` 중복 코드 통합
- `translator.js` 내 `_playVideoOnly()` 제거, `motion_loader_v3.js`의 V3 버전으로 통합
- 단독 재생과 시퀀스 재생의 영상 전환 일관성 확보
- 예상 소요: 반일

#### 7.1.3. 재생 속도 설정 변수화
- `playbackRate` 0.9 하드코딩 → `window.ITDA_CONFIG.playbackRate` 등 전역 설정으로 이동
- 사용자/개발자가 UI에서 속도 조절 가능하도록 준비
- 예상 소요: 반일

### 7.2. 단기 목표 (2주 이내)

#### 7.2.1. Supabase Pro 업그레이드 및 Storage 최적화
- 현재 Free 플랜 (1GB) → Pro 플랜 ($25/월, 100GB) 업그레이드
- 1,113개 webm (~680MB) + 향후 확장 여유 확보
- 불필요한 원본 mp4 정리

#### 7.2.2. 미확보 단어 영상 추가 확보
- 로컬 인덱스 5,987개 중 영상 없는 ~4,800개에 대해:
  - sldict 크롤러 구축 → 추가 단어 영상 수집
  - AI Hub 수어 데이터셋(3,000단어) 다운로드 및 연동 검토
- 일상 대화 핵심 2,000개 우선 목표

#### 7.2.3. 영상 재생 UI 개선
- 재생 중 현재 단어 표시 (자막 형태)
- 재생 진행 바
- 반복 재생 / 느리게 재생 버튼

### 7.3. 중기 목표 (1개월 이내)

#### 7.3.1. 백엔드 전환 분석 도구 (Phase 2)
- `api/tools/transition_analyzer.py` — 모션별 entry/exit 포즈·각속도 사전 분석
- `api/tools/transition_cache_builder.py` — 자주 쓰는 단어 쌍의 전환 keyframe 사전 계산
- `GET /transition/{word_a}/{word_b}` API 엔드포인트

#### 7.3.2. KoNLPy 형태소 분석기 연동
- 현재 규칙 기반 활용어미 스트리핑의 한계 보완
- 복합 불규칙 활용 자동 처리 (예: "지었다"→"짓다", ㅅ불규칙 + 과거)
- 미등록 활용형 자동 발견 및 SYNONYMS 확장

#### 7.3.3. 3D 아바타 복원 (하이브리드 모드)
- 영상이 있는 단어 → 영상 재생 (현재)
- 영상이 없는 단어 → 3D 아바타 키프레임 폴백
- avatar.js / retargeting.js 재활성화 및 영상↔아바타 전환 매끄러운 처리

### 7.4. 장기 목표 (2~3개월)

#### 7.4.1. AI 전환 예측 모델 (Phase 3)
- 경량 Transformer seq2seq (~2M 파라미터)
- 단어 N의 마지막 5프레임 + 단어 N+1의 첫 5프레임 → 전환 keyframe 예측
- Phase 1의 Hermite 전환을 synthetic ground truth로 활용한 학습 데이터 구축
- ONNX 서빙으로 기존 `/transition/` 엔드포인트에 통합

#### 7.4.2. NMS(Non-Manual Signals) 동기화
- 수어의 의미를 보강하는 얼굴 표정(입모양, 눈썹)과 손 동작 정밀 싱크
- 감정 분석 결과를 아바타 표정 모핑에 반영

#### 7.4.3. 대규모 사용자 테스트
- 실제 수어 사용자(농인) 대상 가독성 및 정확도 피드백
- 사용자 입력 로그 분석 → 미등록 단어 자동 발견
- A/B 테스트: 영상 모드 vs 아바타 모드 선호도

#### 7.4.4. 모바일 최적화 및 배포
- PWA 또는 네이티브 앱 래핑
- 오프라인 모드 (IndexedDB 캐시 활용)
- 저대역폭 환경 대응 (영상 압축 최적화)

---

## 8. 결론

Project ITDA는 **기능 구현 단계를 넘어 실용화 단계**에 진입했습니다.

- **수어 영상 DB**: 80개 → **1,113개**로 14배 확장 (일상 대화 수준 달성)
- **문맥 인식 토크나이저**: "사랑합니다", "반가워요", "추워" 등 자연어 입력을 수어 어간으로 정확히 변환
- **부드러운 전환**: Hermite 스플라인 + 적응형 크로스페이드로 단어 간 끊김 해소
- **영상 전환**: 검정 화면 없이 더블 버퍼링 크로스페이드로 자연스러운 연속 재생

다음 목표는 **2,000개+ 영상 확보**, **3D 아바타 하이브리드 모드 복원**, **AI 전환 예측 모델 구축**입니다.

**2026.05.31**
**개발팀 리드: 반디 (Antigravity)**

---

## 문서 이력
- 2026.04.15: 프로젝트 초기 아키텍처 설계
- 2026.04.21: RAG 및 비전 엔진 기초 통합
- 2026.04.22: V2 프로필 및 교육 모드 MVP 완료
- 2026.04.23: V3 실측 데이터셋 파이프라인 구축
- 2026.04.27: V4 정밀 수형 라이브러리 및 최종 시스템 최적화
- 2026.04.28: 불필요한 더미 데이터 삭제(1.8GB 확보) 및 아바타 대기 자세 최적화
- 2026.05.22: Supabase 신 스키마 마이그레이션, 영상 배경 제거 파이프라인 구축
- 2026.05.26: 아바타 관절 데이터 정밀화, 영상 우선 모드 전환, 80개 영상 배경 제거
- 2026.05.27: Hermite 스플라인 전환 시스템, 적응형 크로스페이드, 영상 전환 개선
- 2026.05.28: 문맥 인식 토크나이저 (활용어미/조사/불규칙 처리), DB 대규모 확장 시작
- **2026.05.31: 1,113개 영상 배경 제거 완료, 코드 품질 점검 및 버그 수정, 향후 추진 계획 수립**
