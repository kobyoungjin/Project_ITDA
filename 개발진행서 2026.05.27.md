# 프로젝트 ITDA 개발진행서 (2026.05.27)

본 보고서는 수어 영상 간 부드러운 전환 시스템 구축, 문맥 인식 토크나이저 개선 작업을 정리합니다.

## 1. 주요 개발 성과

### 1-1. 수어 단어 간 부드러운 전환 시스템 (Phase 1)

**문제**: 수어 문장 재생 시 단어와 단어 사이에 250ms 데드 갭이 존재하고, 단어 N의 마지막 프레임에서 단어 N+1의 첫 프레임으로 순간 이동하여 뚝뚝 끊기는 현상이 발생.

**원인 분석**:
- `playNextSign()`에서 비-마지막 단어의 `skipReturn=true` 시 아바타가 마지막 keyframe 자세를 유지한 채 다음 단어로 바로 점프
- 300ms 크로스페이드는 정적 포즈 → 새 애니메이션 블렌딩만 수행, 각속도/모멘텀 연속성 없음
- 250ms 고정 갭(gapMs)이 단어 사이에 빈 시간 생성

**해결 — Hermite 스플라인 전환 프레임 자동 생성**:

| 추가 함수 | 역할 |
|-----------|------|
| `_poseDistance(poseA, poseB)` | 두 포즈 간 평균 각도 거리(라디안) 계산 |
| `_estimateAngularVelocity(keyframes, type)` | 모션 경계에서 각속도 추정 (exit/entry) |
| `_generateTransitionKeyframes(...)` | Hermite 스플라인 기반 전환 keyframe 배열 생성 (smoothstep + 모멘텀 접선) |
| `_playTransitionFrames(avatar, kfs, dur)` | rAF 기반 전환 micro-animation 재생 |
| `_peekNextMotion()` | 큐에서 다음 단어 모션 데이터 미리보기 |

**동작 흐름**:
```
단어 A 재생 완료
  → _peekNextMotion()으로 단어 B 모션 확인
  → _poseDistance()로 A 종료 포즈 ~ B 시작 포즈 거리 측정
  → 거리 기반 전환 시간 결정 (80ms ~ 350ms 적응형)
  → _generateTransitionKeyframes()로 Hermite 전환 프레임 생성
  → _playTransitionFrames()로 부드러운 micro-animation 재생
  → 단어 B 재생 시작 (적응형 크로스페이드: 포즈가 이미 가까우므로 짧음)
```

**파일**: `frontend/js/motion_loader_v3.js`

### 1-2. 적응형 크로스페이드

**문제**: 모든 단어 시작 시 고정 300ms 크로스페이드 — 포즈가 가까우면 과도하고, 멀면 부족.

**해결**: `_poseDistance()`로 이전 자세와 첫 keyframe 간 거리를 측정, 100ms~500ms 범위에서 자동 조절.

```
거리 작음 (전환 micro-animation 직후) → ~100ms 크로스페이드
거리 큼 (단독 재생 등) → ~500ms 크로스페이드
```

**파일**: `frontend/js/motion_loader_v3.js`

### 1-3. 영상(Video) 전환 개선

**문제**: 영상 기반 단어 재생 시 ① 영상 시작/종료에 검정 화면 노출, ② 150ms 크로스페이드가 너무 짧음, ③ 이미 크로스페이드로 재생 중인 영상을 다시 로드하여 깜빡임 발생.

**원인**: `_playVideoOnly()`가 호출될 때마다 `activeVid.src = videoUrl` + `load()`를 실행 — 더블 버퍼링으로 이미 재생 중인 영상도 다시 로드하여 끊김 발생.

**해결**:

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 크로스페이드 시간 | 150ms | 350ms |
| trimEnd (끝 차렷 구간 자르기) | 0.5s / 0s | 1.8s (전 영상 동일) |
| 마지막 영상 종료 | 800ms 검정 페이드아웃 | 즉시 전환 (검정 화면 없음) |
| 이미 재생 중인 영상 | src 재설정 → 리로드 → 깜빡임 | `alreadyPlaying` 감지 → src 재설정 스킵 |
| 영상 시작 표시 | 즉시 (검정 프레임 노출) | `playing` 이벤트 시 표시 (첫 프레임 렌더 후) |
| 기본 단어 간 갭 | 250ms | 0ms (전환 애니메이션이 대체) |

**파일**: `frontend/js/motion_loader_v3.js`

### 1-4. 문맥 인식 토크나이저 — 활용어미·조사 스트리핑

**문제**: "사랑합니다" 입력 시 토크나이저가 "사랑" + unknown("합니다")로 분절. "감사합니다"도 동일. 한국 수어(KSL)는 문법 어미를 표현하지 않으므로 어간만 추출하면 됨.

**해결 — 3단계 fallback 파이프라인**:

```
입력 "사랑합니다"
  → ① 사전에서 통째 검색 → 없음
  → ② _strip_and_lookup(): suffix "합니다" 제거 → "사랑" → 사전 매칭 ✅
  → 결과: {word: "사랑합니다", canonical: "사랑"}
```

**활용어미 패턴 (60+ 종)**:
- 격식체: ~합니다, ~습니다, ~습니까, ~합니까
- 비격식: ~해요, ~어요, ~아요
- 반말: ~해, ~어, ~아
- 과거: ~했다, ~었다, ~았다
- 종결: ~구만, ~구나, ~군요, ~네요, ~죠, ~잖아, ~거든, ~더라고
- 인용: ~다고, ~라고, ~다며, ~다면서
- 연결: ~면서, ~니까, ~지만, ~어서, ~려고
- 관형/부사: ~게, ~운, ~은, ~ㄴ, ~을

**조사 패턴 (22종)**:
- ~은/는, ~이/가, ~을/를, ~에서, ~에게, ~까지, ~부터, ~조차, ~의 등

**파일**: `api/services/motion_tokenizer.py`

### 1-5. 불규칙 활용 처리

**문제**: "반가워", "추워", "몰라" 등 불규칙 활용형은 어간 자체가 변하므로 단순 suffix 스트리핑으로 원형 복원 불가.

| 불규칙 유형 | 예시 | 어간 변화 |
|------------|------|----------|
| ㅂ불규칙 | 반갑다→반가워 | 갑→가 |
| ㄷ불규칙 | 걷다→걸어 | 걷→걸 |
| 르불규칙 | 모르다→몰라 | 모르→몰 |
| ㅎ불규칙 | 파랗다→파래 | 랗→래 |

**해결 — 이중 안전망**:

1. `_IRREGULAR_STEMS` 매핑 테이블 (motion_tokenizer.py)
   - suffix 스트리핑 후 어간이 사전에 없으면 불규칙 매핑으로 원형 복원
   - 예: "반가" → "반갑다", "추" → "춥다", "몰" → "모르다"

2. `SYNONYMS` 테이블 확장 (supabase_service.py)
   - 주요 불규칙 활용형을 직접 등록 (150+ 엔트리)
   - 각 ㅂ불규칙 용언에 기본형/반말/높임/과거/관형/부사형 포함
   - 예: 반가워, 반가워요, 반가웠어, 반가웠습니다, 반가운, 반갑게 → 모두 "반갑다"

**파일**: `api/services/motion_tokenizer.py`, `api/services/supabase_service.py`

### 1-6. 토크나이저 처리 검증

| 입력 | 처리 경로 | 결과 |
|------|----------|------|
| 사랑합니다 | suffix "합니다" → "사랑" | **사랑** |
| 감사합니다 | suffix "합니다" → "감사" | **감사** |
| 사랑해요 | suffix "해요" → "사랑" | **사랑** |
| 반갑습니다 | suffix "습니다" → "반갑" → "반갑다" | **반갑다** |
| 반가워요 | SYNONYMS 직접 | **반갑다** |
| 반갑구만 | suffix "구만" → "반갑" → "반갑다" | **반갑다** |
| 반갑다고 | SYNONYMS + suffix "다고" | **반갑다** |
| 추워요 | SYNONYMS 직접 | **춥다** |
| 어려워 | SYNONYMS 직접 | **어렵다** |
| 학교에서 | 조사 "에서" → "학교" | **학교** |
| 좋구만 | suffix "구만" → "좋" → "좋다" | **좋다** |
| 피곤하구만 | suffix "구만" → "피곤하" → "피곤하다" | **피곤하다** |

## 2. 현재 시스템 상태

### 아키텍처 변경점
```
[이전]
사용자 입력 → 최장일치 토크나이저 → 단어 배열 → 순차 재생 (250ms 갭)

[현재]
사용자 입력 → 문맥 인식 토크나이저 (최장일치 + 어미/조사 스트리핑 + 불규칙 복원)
  → 단어 배열 → 영상: 더블 버퍼링 + 350ms 크로스페이드 (검정 화면 없음)
             → 아바타: Hermite 전환 프레임 자동 생성 + 적응형 크로스페이드
```

### 수정된 파일 목록

| 파일 | 변경 내용 |
|------|----------|
| `frontend/js/motion_loader_v3.js` | 전환 함수 5개 추가, _playCore/playNextSign 수정, 영상 전환 개선 |
| `api/services/motion_tokenizer.py` | 활용어미 60+종, 조사 22종, 불규칙 어간 30+개 매핑, _strip_and_lookup 추가 |
| `api/services/supabase_service.py` | SYNONYMS 83개→150+ 확장 (ㅂ/ㄷ/르/ㅎ 불규칙 활용형 전부 등록) |

## 3. 차후 단계 (Next Steps)

### Phase 2: 백엔드 전환 분석 도구
1. `api/tools/transition_analyzer.py` — 모션별 entry/exit 포즈·각속도 사전 분석
2. `api/tools/transition_cache_builder.py` — 자주 쓰는 단어 쌍의 전환 keyframe 사전 계산
3. `GET /transition/{word_a}/{word_b}` API 엔드포인트 — 사전 계산 캐시 또는 실시간 계산 제공

### Phase 3: AI 전환 예측 모델 (설계 완료)
1. 경량 Transformer seq2seq (~2M 파라미터) — 단어 N의 마지막 5프레임 + 단어 N+1의 첫 5프레임 → 전환 keyframe 예측
2. Phase 1의 Hermite 전환을 synthetic ground truth로 활용한 학습 데이터 구축
3. ONNX 서빙으로 기존 `/transition/` 엔드포인트에 통합

### 토크나이저 추가 개선
1. KoNLPy 형태소 분석기 연동 — 불규칙 활용 자동 처리 확대
2. 사용자 입력 로그 기반 미등록 활용형 자동 발견 및 SYNONYMS 확장
