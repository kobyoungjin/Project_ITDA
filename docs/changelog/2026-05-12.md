# 변경 내역 — 2026-05-11

> 커밋: `1abbcd74` (09:25) · `f9725fa4` (17:48)  
> 브랜치: `test` → `dodreamai`

---

## 🧠 1. KNN 모델 & 학습 데이터 개선

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 학습 데이터 품질 | 중복 포함, 클래스 불균형 | 중복 제거 + 클래스당 최대 100개 제한 |
| 데이터 증강 | 없음 | 회전 / 크기 변환 / 노이즈 → 프레임 1개 → **9배** 자동 생성 |
| 모델 재훈련 반영 | 서버 재시작 필요 | **즉시 반영** (`reload_model()`) |

**증강 방식** (`api/core/ml_utils.py` · `augment_landmarks`):
- 2D XY 미세 회전 (±15°)
- 스케일 변동 (±15%)
- XY 가우시안 노이즈 (σ = 0.01)
- Z축 깊이 노이즈 (σ = 0.005)

**추가된 스크립트**:
- `retrain_aug.py` — 증강 포함 재훈련
- `direct_train.py` — 직접 훈련
- `diagnose_knn.py` — KNN 진단
- `diagnose_sarang.py` — 특정 단어 진단
- `train_script.py` / `reload_model.py` / `retrain_target.py`

---

## ⚡ 2. 인식 속도 개선 (`api/routers/ws_vision.py`)

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| EMA 필터 강도 | 0.4 | **0.6** (더 즉각 반응) |
| 투표 윈도우 크기 | 10프레임 | **5프레임** |
| 투표 인정 기준 | 3회 일치 | **2회 일치** |
| 처리 중 프레임 | 드랍(버림) | **최신 프레임 보관** 후 재처리 |
| 모션 감지 시 잔상 | 남음 | 투표창 자동 초기화 🧹 |
| Full pipeline 조건 | `stable` or `idle` | **`stable` 만** |

**추가된 상태값**:
- `pending_frames` — 대기 중 최신 프레임 보관
- `last_motion_phase` — 상태 전환 감지 (잔상 제거용)
- `sentence_buffer` / `last_action_time` — 문장 구성기(Sentence Builder) 준비

---

## 🤖 3. SLM 판단 로직 개선 (`api/services/slm_agent.py`)

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| 후보 단어 목록 | 하드코딩 16개 + KNN 단어 | **KNN 학습 단어만** (오판 방지) |
| idle 상태 처리 | 손 없을 때만 대기 중 | 유효 구역(얼굴 / 가슴) **밖** 이면 대기 중 |
| 출력 정제 | 기본 | `"정답:"` 접두어 자동 제거 |

---

## 👁️ 4. 카메라 / 시각화 (`frontend/js/vision.js`)

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| Pose 모델 | `pose_landmarker_lite` | **`pose_landmarker_full`** |
| Pose 신뢰도 임계값 | 0.5 | **0.6** |
| 손 연결선 색상 | 하늘색 `#00F2FE` | **마젠타 네온** `#FF00FF` (글로우 효과) |

> Pose full 모델로 교체한 이유: 손이 교차하거나 겹치는 상황에서 정확도 향상

---

## 📊 영향 범위 요약

```
api/core/ml_utils.py          ← 데이터 증강 함수 추가
api/routers/ws_vision.py      ← 속도 개선 / 문장 구성기 준비
api/routers/collect.py        ← 증강 적용 + 모델 즉시 갱신
api/services/slm_agent.py     ← 후보 단어 / idle 처리 개선
api/services/knn_classifier.py← reload_model() 추가
api/data/ksl_training/        ← knn_model.pkl, ksl_dataset.csv 갱신
frontend/js/vision.js         ← Pose full 모델 / UI 개선
```
