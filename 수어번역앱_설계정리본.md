# 수어 번역 애플리케이션 — 설계 정리본

> 음성·텍스트 입력 → 한국수어(KSL)를 3D 아바타 모션으로 출력하는 앱의 전체 설계 문서.

---

## 개요

- **본질**: 단순 단어 치환이 아니라 "번역 + 모션 합성". 한국어와 한국수어(KSL)는 문법이 다른 별개 언어다.
- **어려운 두 곳**: (1) 한국어 → 수어 글로스 변환, (2) 영상에서 아바타 구동용 모션 확보.
- **기대치**: 완전히 자연스러운 KSL은 연구 수준. MVP는 "의미 전달"을 목표로 하고, 농인·수어통역사 검수를 초기부터 받는다.

---

## 전체 파이프라인

```
음성 → STT → 한국어 텍스트 → 자연어 전처리(형태소)
→ 글로스 변환(어순 재배치, 핵심 난제)
→ 모션 매핑(클립 연결 + 보간)
→ 3D 아바타 출력(손 + 비수지 채널)
```

---

## 1. 입력 · 글로스 변환

- **입력**: 음성은 Whisper / Clova STT, 텍스트는 그대로.
- **글로스 변환**: 조사 제거 + 어순 재배치가 핵심. 단순 사전 매칭만 하면 부자연스러운 "수지한국어"가 나온다.
  - MVP: 형태소 분석(KoNLPy/Mecab) + 어순 규칙 + 사전 매칭
  - 고도화: 한국어–수어글로스 병렬 말뭉치로 seq2seq / KoBART 파인튜닝

---

## 2. 데이터

### AI Hub 한국수어 데이터셋 (핵심 자원)

- 약 536,000 클립. 키포인트 제공: 양손 각 21, Pose 25, Face 68.
  - **단, 키포인트는 2D `[x, y, 신뢰도]` 형식.**
- 형태소·비수지 가공값(JSON)도 함께.
  - **비수지 가공값은 좌표가 아니라 시간구간별 "라벨"** (예: `"의문사없는 의문형"`).
- 별도 데이터셋에 **Pointer Model**(한국어 문장 → 지화로 표현할 단어 추출) → OOV 판별 출발점.
- 지문자: 지숫자/지문자 약 1,000 + 지문자 영상 약 21,000클립 + 키포인트 포함.
- 주의: 큰 데이터셋에 묶임(분할압축, 리눅스 권장). 빠른 손동작·지문자 구간 키포인트 품질 검수 필요.

### 없는 수어 추가

- **0단계: 발명 금지.** 국립국어원 한국수어사전(sldict.korean.go.kr)에서 먼저 정립된 수어 확인.
- 모션 생성 4가지:
  - 영상 촬영 + 좌표 추출 (기본)
  - 모션캡처 (최상·고비용·핸드 글러브 필수)
  - 수동 애니메이팅 (소수 단어)
  - 표기법 기반 합성 (HamNoSys·SiGML, 확장성 최고)
- 운영: "영상 → 좌표추출 → 농인 검수 → 등록" 자동 파이프라인으로.

### 지문자 / OOV

- 자모 30여 개 핸드셰이프만 깨끗이 만들면 어떤 단어든 조합 가능. 처음부터 폴백으로 내장.
- 단어 수어 vs 지문자 분기는 Pointer Model 데이터 활용.

---

## 3. 모션 출력 · 보간

- **VRM 포맷** 사용: 손가락 본이 표준 휴머노이드 골격에 정의돼 모션 이식이 표준화됨.
- **데이터 → 손가락**: AI Hub 21점 손 키포인트 → 관절 회전값 변환 → VRM 본.
  - **Kalidokit** Hand 솔버가 그 변환 담당 (실시간 트래킹용·유지보수 비활발 → 변환 로직만 참고해 재생용으로 재사용, 스무딩 필수).
- **보간 (필수)**:
  - 전이 보간: 관절 회전 SLERP + ease-in-out. MVP는 고정 0.2~0.3초로 충분.
  - 코아티큘레이션(고도화): 가변 전이 길이, 손위치/손모양/방향 타이밍 분리, 연속 발화 데이터 학습.
  - AI Hub 단어는 고립 발화(citation form) → 양 끝의 과장된 준비/마무리 동작 트리밍 후 전이.
  - 비수지 채널은 **별도 타임라인** (눈썹·머리는 여러 단어에 걸쳐 유지 → 손 타임라인 따라 끊지 말 것).

---

## 4. 아바타 만들기

- **공통 출구**: 어떤 툴이든 Blender + VRM 애드온을 거쳐 VRM이 된다. 커스텀 입 형태소를 어차피 Blender에서 넣으므로 Blender 작업은 불가피.

### 방향 

- **권장 — 스타일라이즈드된 사람 얼굴**: VRoid Studio(무료)로 정상에 가까운 비율의 캐릭터 제작 → VRM 바로 출력(손가락 본 + 표준 표정 포함). 웹 스택(three-vrm)과 가장 잘 맞물림.
- **사실적 사람 얼굴**: Character Creator 4 → Blender(무료 Auto Setup) → VRM. 스켈레톤·블렌드셰이프(ARKit→VRM) 재매핑 필요. 변환 마찰이 크고, 렌더를 Unity로 두는 게 나을 수 있음. 언캐니·가독성 문제로 수어엔 불리할 수 있음.
- **공통 주의 — 손 가독성 최우선**: 어떤 스타일이든 **손은 크고 또렷하게** 유지. 손모양·지문자 가독성이 수어의 핵심.

### 직접 그려넣기 = 텍스처 그리기

- 2D 스프라이트를 얹는 게 아니라 메시 UV에 그림을 입히는 것. VRoid 얼굴은 눈·입이 사실상 2D 텍스처라, 직접 그리면 "내가 그린 얼굴"이 된다.
- 방법:
  - VRoid 내장 텍스처 에디터 (펜압·레이어, 3D/UV 직접 드로잉)
  - 외부(Procreate / CLIP STUDIO / 포토샵)에서 그려 임포트
- 비율은 메시·슬라이더가, 화풍은 텍스처가 결정 — 둘은 독립.
- 진짜 2D 스프라이트(Live2D)는 별개 파이프라인이고 수어엔 부적합(손 깊이·겹침 표현 불가).

---

## 5. 비수지신호 (얼굴 · 머리) — 핵심 축

- **본 vs 블렌드셰이프**: 몸·손 = 본, 얼굴 표정 = 블렌드셰이프(별개 시스템). 머리는 본 1개(끄덕임·젓기 = 머리 본 회전).
- 얼굴 메시·텍스처 + 표준 표정(감정·깜빡임·시선·입모양 비짐)은 VRoid에 **이미 포함** → 새로 그릴 필요 없음.

### 표정 만드는 법

- **기본 = 메시 변형(블렌드셰이프)**: 얼굴은 한 번만 그리고 입체를 눌러 모양 변경. "모든 입짓을 그릴" 필요 없음. 표준 표정은 공짜로 따라옴.
- 직접 손대는 건 **수어 입 형태소 소수**뿐. 그것도 그림이 아니라 Blender 셰이프 키로 한 번씩 조형.
- 텍스처 스왑(입모양마다 다른 그림)은 더 손 가는 선택지 → 화풍상 꼭 필요할 때만.

### 입짓(립싱크) 규칙

- **음성 립싱크 금지**: TTS·문장 음소로 입을 만들면 문법용 입 채널을 빼앗고 수지한국어로 치우침.
- **수어 고유 입은 필수**:
  - 마우싱(단어별, 중의성 해소)
  - 입 형태소(부사·문법적, 일부는 단어 의미에 필수 — 손이 같아도 입이 다르면 다른 단어)
- 규칙: 입은 **수어 단어 클립이 데이터에서 가져온 것**으로 구동. 글로스 단위에 묶고 오디오/TTS 파이프라인과 완전 분리.
- 예외: 지문자·커스텀 단어는 입 데이터가 없으면 중립으로 두거나 의도적 마우싱 지정 (자동 TTS 생성은 절대 아님).

### AI Hub 데이터와의 어우러짐

- 이상해지는 건 메시 변형 자체가 아니라 **2D 좌표 → 가중치 변환** 구간이다.
- AI Hub 얼굴 데이터는 두 종류 → 다르게 배분:
  - **비수지 라벨**(문법 표지: 의문·부정·눈썹·머리) → "라벨 = 블렌드셰이프 규칙"으로 깔끔하게 구동. 좌표를 안 풀어서 노이즈 0.
  - **미세 입 형태소**(오므림·볼·th 등 3D성) → 라벨이 종류를 알려주니, 좌표 복원하지 말고 손으로 만든 형태소 블렌드셰이프를 트리거.
  - **연속 동작**(깜빡임·입 벌림 정도) → 68점 2D 키포인트로 채우되 정규화 + 스무딩.
- 2D 키포인트 약점: 깊이 없음(오므림·볼 복원 안 됨), 68점은 성김, 캐릭터별 보정 필요.
- **클립별로 한 번 구워 검수** (실시간 raw 변환 금지) → 이상한 프레임을 출시 전에 제거.
- 데이터 한계: 모든 구간에 비수지 라벨이 꽉 차 있진 않을 수 있음 → 빈 구간은 중립.

---

## 6. 기술 스택

| 영역 | 선택 |
|---|---|
| 프론트 언어 | TypeScript |
| 프론트 프레임워크 | React + react-three-fiber |
| 렌더(웹) | Three.js + @pixiv/three-vrm (r3f 통해 사용) |
| 렌더(고품질·네이티브) | Unity (C#) |
| ML·백엔드 | Python(FastAPI), MediaPipe Holistic, PyTorch |
| STT | Whisper / Clova |
| NLP | KoNLPy, Mecab, KoBART |
| 아바타 | VRM (VRoid Studio 생성) + Blender(VRM 애드온) |
| 키포인트→리그 | Kalidokit Hand/Face 솔버 (또는 그 방식) |

---

## 7. 단계별 로드맵

1. **루프 닫기**: 텍스트 → 핵심 단어 100개 사전 매칭 → 웹 아바타 순서 재생(고정 SLERP 전이).
2. STT, 어순 규칙, 지문자 OOV 폴백, 비수지 라벨 기본 매핑(의문·부정).
3. AI Hub로 어휘·모션 확장, 글로스 ML화, 커스텀 입 형태소, 가변 전이·코아티큘레이션, 농인 검수 반복.

---

## 8. 관통 원칙

- 수어는 농인 커뮤니티의 언어 → 정의·검수에 농인·수어통역사 필수.
- 입은 음성에서 분리, 수어 단어에서 온 것만.
- 손 가독성 최우선 (스타일라이즈드해도 손은 또렷이).
- 비수지 문법 표지는 좌표가 아니라 **라벨**에서 가져와 변환 노이즈 회피.
- KSL ≠ ASL: 입 형태소 목록 등은 KSL 자료·검수로 확인.
