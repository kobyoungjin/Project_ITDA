# 프로젝트 ITDA 개발진행서 (2026.05.26)

본 보고서는 수어 영상 기반 구현 전환, 배경 제거 파이프라인 구축, 아바타 관절 데이터 정밀화 작업을 정리합니다.

## 1. 주요 개발 성과

### 1-1. 아바타 관절 데이터 정밀화 (REST_DIRECTIONS GLB 실측)

**문제**: 기존 `mediapipe_retarget.py`의 `REST_DIRECTIONS`가 모든 손가락 본을 순수 ±X 방향(`[-1,0,0]`)으로 가정하여, 특히 엄지(Thumb)의 회전이 45도 이상 부정확했음.

**해결**: `pygltflib`로 `sonyr.glb` 모델의 실제 bone world position을 추출하여 parent→child 벡터를 계산, 37개 본 전체의 rest direction을 GLB 실측값으로 교정.

| 본 | 이전 (추정값) | 교정 후 (GLB 실측) | 오차 |
|---|---|---|---|
| RightHandThumb1 | (-1.0, 0, 0) | (-0.66, -0.26, +0.70) | 45도+ |
| RightHandThumb2 | (-1.0, 0, 0) | (-0.86, -0.46, +0.23) | 30도+ |
| RightHand | (-1.0, 0, 0) | (-0.96, -0.05, +0.28) | 16도 |
| Neck | (0, 1, 0) | (0, +0.98, +0.21) | 12도 |

**파일**: `api/tools/mediapipe_retarget.py` REST_DIRECTIONS 섹션

### 1-2. 손목 보정 일원화 (Bake Retarget Correction)

**문제**: 매 프레임 재생 시 `_applyRetargetCorrection()`이 소스별/단어별 보정을 적용 — `감사` 단어 하드코딩 예외, 소스 누락 시 보정 누락 등 불안정.

**해결**: 모션 로드 시점에서 `_bakeRetargetCorrection()`으로 Hand 본 쿼터니언에 보정을 1회 미리 적용. 재생 시에는 보정 없이 순수 Slerp만 수행.

**파일**: `frontend/js/motion_loader_v3.js`

### 1-3. Supabase 모션 손가락 자동 보완

**문제**: Supabase DB 모션 데이터 중 일부가 7개 본(Spine + 양팔)만 포함, 손가락 30개 본 누락.

**해결**: `_applyInterpolated()`에서 keyframe에 손가락 본이 없으면 `linguistic_features.handshape` 또는 기본 수형(`5지`)을 자동 적용.

**파일**: `frontend/js/motion_loader_v3.js`

### 1-4. 영상 배경 제거 파이프라인 (rembg U2-Net)

**문제**: 수어 교육 영상의 배경이 아바타 위에 겹쳐 보임. MediaPipe Selfie Segmentation은 손가락/머리 가장자리에서 배경 잔상 발생.

**해결**: `rembg` (U2-Net 기반) + alpha matting 적용으로 정밀 배경 제거. 3-Zone Soft Alpha 방식 도입.

| 방법 | 가장자리 품질 | 처리 속도 |
|------|------------|----------|
| MediaPipe (이전) | 배경 잔상 있음 | ~0.13초/프레임 |
| rembg U2-Net (현재) | 정밀 매팅 | ~1.2초/프레임 |

**파일**: `api/tools/remove_video_bg_test.py`

### 1-5. Supabase 80개 영상 배치 배경 제거 + 업로드

**작업**: `sign_videos` 버킷의 mp4 80개를 rembg로 배경 제거 → transparent webm 생성 → Supabase Storage 업로드 → `sign_source_video.video_url` 업데이트.

| 항목 | 결과 |
|------|------|
| 처리 대상 | 80개 mp4 |
| 성공 | 80개 |
| 실패 | 0개 |
| DB 업데이트 | 85개 (기존 5개 포함) video_url → webm |

**파일**: `api/tools/batch_remove_bg.py`

### 1-6. 영상 우선 모드 전환

**결정**: 아바타 관절 데이터 정밀도 한계로 인해, 수어 영상이 있는 단어는 **영상을 우선 재생**하도록 전환.

**구현**:
- `loadMotion()`: Supabase에서 `video_url`이 있으면 모션(keyframes)보다 영상 우선 반환
- `playMotion()`, `playSequence()`: keyframes 없고 video_url만 있으면 `_playVideoOnly()` 호출
- `supabase_service.py`: 모션 데이터 없어도 video_url만 있으면 4단계에서 반환
- `index.html`: Three.js 캔버스 및 avatar.js/retargeting.js 비활성화

**파일**: `frontend/js/motion_loader_v3.js`, `frontend/js/translator.js`, `api/services/supabase_service.py`, `frontend/index.html`

## 2. 현재 시스템 상태

- **영상 재생 가능 단어**: 80개 (투명 배경 webm)
- **모션(아바타) 데이터**: 비활성 (영상 우선 모드)
- **3D 아바타**: 비활성 (복원 가능 — index.html 주석 해제)

### 데이터 연결 파이프라인
```
사용자 입력 → 토크나이저 → Supabase 모션 조회
  → video_url 있음 → 투명 webm 영상 전체 화면 재생
  → video_url 없음 → "모르다" 폴백
```

### 영상 현황
| 구분 | 수량 | 상태 |
|------|------|------|
| 영상+모션 모두 있는 단어 | 46개 | 영상 우선 재생 |
| 영상만 있는 단어 (모션 X) | 34개 | 영상 재생 |
| 모션만 있는 단어 (영상 X) | 7개 | 아바타 비활성으로 재생 불가 |
| 전체 영상 | 80개 | 투명 배경 webm |

### 미적재 7개 단어
| 단어 | 상태 | 대응 방안 |
|------|------|----------|
| 날씨 | 모션 O, 영상 X | sldict 수동 검색 필요 |
| 맑다 | 모션 O, 영상 X | "맑음" 변형 검색 |
| 배려 | 모션 O, 영상 X | "배려하다" 변형 검색 |
| 서툴다 | 모션 X, 영상 X | "서투르다" 변형 검색 |
| 음식 | 모션 O, 영상 X | sldict 수동 검색 필요 |
| 청인 | 모션 O, 영상 X | sldict 수동 검색 필요 |
| 화면 | 모션 O, 영상 X | sldict 수동 검색 필요 |

## 3. 차후 단계 (Next Steps)

1. **미적재 7개 단어 영상 확보**: sldict 수동 검색 또는 동의어 변형 검색
2. **영상 재생 UI 개선**: 재생 중 단어 표시, 진행 바, 반복 재생 등
3. **아바타 복원 옵션**: 영상이 없는 단어에 대해 아바타 폴백 재활성화 검토
4. **추가 단어 영상 확보**: 80개 외 추가 단어에 대한 영상 수집 및 배경 제거
