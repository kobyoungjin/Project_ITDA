# 🤝 Project ITDA(잇다): AI 기반 수어 번역 및 교육 플랫폼
## [개발 진행 보고서] — 2026.05.13 최종 업데이트 (KSL 데이터셋 자동화 및 인식 엔진 고도화)

본 보고서는 2026.05.12 보고서의 인식 엔진 정밀화 성과를 바탕으로, **KSL 제스처 데이터셋 파이프라인 자동화 및 고정밀 3D 모션 데이터 통합(2026.05.13)** 성과를 통합하여 작성된 문서입니다.

---

## 1. 프로젝트 개요 (Project Overview)
- **프로젝트 명**: ITDA (잇다)
- **핵심 목표**: 청각장애인과 비장애인의 원활한 소통을 위해 AI(SLM/RAG/Vision) 기술을 활용한 실시간 수어 번역 및 교육용 아바타 플랫폼 구축.
- **최상위 설계 원칙**: **"수화 캐릭터가 수화를 정확히 표현하고, 시스템이 사용자의 수어를 정확히 인식하는 것."** 단순한 동작 흉내를 넘어, 해부학적 정확도와 표준 수형 규격(KSL)을 준수하는 고정밀 양방향 통번역 환경 구현.

---

## 2. 전체 개발 진행률 (Overall Progress)

기존 인식 엔진에 고정밀 학습 데이터를 대량 공급하고, 실시간 추적 안정성을 확보하여 상용 수준의 엔진 완성도 달성.

| 단계 | 개발 항목 | 진행 상태 | 비고 |
| :--- | :--- | :---: | :--- |
| **1-13단계** | 아키텍처 및 기본 UI 통합 | 완료 | V5 통합 완료 |
| **14단계** | KSL 제스처 데이터셋 자동화 파이프라인 | **완료** | **[NEW]** 20개 핵심 단어 랜드마크 추출 및 자동 검증 |
| **15단계** | 인식 엔진 피처 엔지니어링 고도화 | **완료** | **[NEW]** 정규화된 기하학적 벡터(Normalized Geometric Vectors) |
| **16단계** | 실시간 추적 안정성 강화 | **완료** | **[NEW]** MediaPipe 신뢰도 임계값 최적화 및 추적 연속성 확보 |

---

## 3. 핵심 기술 개발 상세 (2026.05.13 업데이트)

### 3.1. KSL 데이터셋 파이프라인 자동화 (`api/services/motion_extractor.py`, `api/services/data_pipeline.py`)
- **20개 핵심 단어 데이터셋 구축**: 교육용 핵심 어휘 20종에 대해 고품질 랜드마크 데이터를 추출하고 수동 검수 단계를 자동화함.
- **고정밀 3D 모션(JSON) 통합**: 아바타 피드백을 위해 랜드마크 데이터를 Three.js 호환 JSON 포맷으로 변환하여 프론트엔드(`frontend/data/ksl_motions/`)에 즉시 공급하는 체계 구축.
- **자동 검증(Verification) 로직**: 추출된 데이터의 프레임 무결성 및 유효성을 체크하여 학습 데이터의 품질을 보장.

### 3.2. 인식 엔진 피처 엔지니어링 고도화 (`api/core/ml_utils.py`)
- **정규화된 기하학적 벡터(Normalized Geometric Vectors)**: 단순 좌표값이 아닌, 관절 간의 상대적 각도와 거리를 정규화하여 사용자 체형과 카메라 거리 변화에도 강건한 인식 성능 확보.
- **KNN 50단어 모델 최적화**: 고품질 데이터셋을 활용하여 KNN 모델을 재학습함으로써, 유사 동작 간의 혼동을 최소화하고 실시간 추론 정확도를 대폭 개선.

### 3.3. 실시간 추적 및 예측 인터페이스 안정화 (`frontend/js/vision.js`)
- **MediaPipe 신뢰도 최적화**: 랜드마크 추적 임계값(Confidence Threshold)을 미세 조정하여 빠른 손동작에서도 스켈레톤이 끊기지 않고 연속적으로 유지되도록 개선.
- **데이터 흐름 최적화**: 프론트엔드와 백엔드 간의 특징 벡터 전송 효율을 높여 레이턴시를 최소화함.

---

## 4. 시스템 아키텍처 (2026.05.13 갱신)

```text
┌─ Frontend (port 3000) ─────────────────────────────────┐
│  index.html / avatar_only.html                          │
│  ├─ avatar.js          : Three.js 고정밀 렌더링          │
│  ├─ motion_loader_json : [NEW] 고정밀 JSON 모션 로딩     │
│  └─ vision.js          : [NEW] MP Vision 임계값 최적화   │
└────────────────────────────────────────────────────────┘
                         │
                  WebSocket (8000)
                         │
┌─ Backend (port 8000) ─────────────────────────────────┐
│  api/main.py                                           │
│  ├─ services/knn_classifier: [NEW] 50단어 최적화 모델    │
│  ├─ services/data_pipeline : [NEW] 자동화 파이프라인     │
│  └─ ml/ml_utils.py     : [NEW] 기하학적 벡터 정규화      │
└────────────────────────────────────────────────────────┘
```

---

## 5. 🚨 향후 과제 (Remaining Tasks)

1. **에지 케이스 수집**: 조명 조건이 불량하거나 손이 겹치는(Occlusion) 상황에서의 인식 복원력 강화.
2. **모바일 최적화**: 웹 브라우저 기반 실시간 추적의 연산 부하를 줄여 모바일 환경에서도 원활한 구동 보장.
3. **피드백 UI 고도화**: 사용자의 동작이 아바타와 얼마나 일치하는지 시각적인 점수(Score) 또는 히트맵으로 보여주는 기능.

---

## 6. 결론

Project ITDA는 2026.05.13 업데이트를 통해 **데이터 수집-학습-인식-표현**으로 이어지는 전체 파이프라인의 자동화와 고도화를 달성했습니다. 특히 정규화된 기하학적 벡터 도입과 데이터셋 검증 자동화는 향후 수천 단어 이상의 대규모 수어 사전 구축을 위한 기술적 토대를 마련한 중요한 성과입니다.

**작성자**: 개발팀 리드 반디 (Antigravity)
**통합 날짜**: 2026.05.13

---

## 🧭 문서 이력
- 2026.04.15: 프로젝트 초기 아키텍처 설계
- 2026.05.08: 양손 3D 인식 엔진 및 자동 학습 파이프라인 구축
- 2026.05.12: 인식 엔진 정밀도 고도화(코사인 유사도), 아바타 가이드 시스템 적용
- **2026.05.13 (최종 통합): KSL 데이터셋 파이프라인 자동화, 피처 엔지니어링 고도화 및 실시간 추적 안정화**
