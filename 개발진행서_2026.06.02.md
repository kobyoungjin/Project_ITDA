# [ITDA] 한국 수어 인식 프로젝트 개발 진행서 (통합본)

## 📅 최종 업데이트: 2026.06.02

# 🤝 Project ITDA(잇다): AI 기반 수어 번역 및 교육 플랫폼
## [개발 진행 보고서] — 2026.05.12 최종 업데이트 (인식 엔진 고도화 및 아바타 가이드 시스템 통합)

본 보고서는 2026.05.08 보고서의 양손 3D 인식 엔진 구축 성과를 바탕으로, **인식 엔진의 정밀도 최적화(코사인 유사도 도입) 및 아바타 기반의 실시간 수어 교육 가이드 시스템(2026.05.12)** 성과를 통합하여 작성된 문서입니다.

---

## 1. 프로젝트 개요 (Project Overview)
- **프로젝트 명**: ITDA (잇다)
- **핵심 목표**: 청각장애인과 비장애인의 원활한 소통을 위해 AI(SLM/RAG/Vision) 기술을 활용한 실시간 수어 번역 및 교육용 아바타 플랫폼 구축.
- **최상위 설계 원칙**: **"수화 캐릭터가 수화를 정확히 표현하고, 시스템이 사용자의 수어를 정확히 인식하는 것."** 단순한 동작 흉내를 넘어, 해부학적 정확도와 표준 수형 규격(KSL)을 준수하는 고정밀 양방향 통번역 환경 구현.

---

## 2. 전체 개발 진행률 (Overall Progress)

기존 양손 3D 인식 엔진 도입 이후, 실제 사용자 환경에서의 인식 정확도 극대화 및 학습 가이드 UI/UX 완성.

| 단계 | 개발 항목 | 진행 상태 | 비고 |
| :--- | :--- | :---: | :--- |
| **1-9단계** | 아바타 렌더링 및 번역 엔진 기본 | 완료 | V4 통합 완료 |
| **10단계** | 양손 3D KSL 인식 엔진 도입 | 완료 | 35차원 통합 벡터 및 자동 학습 |
| **11단계** | 인식 엔진 정밀도 고도화 | **완료** | **[NEW]** 0~1 정규화 및 코사인 유사도 도입 |
| **12단계** | 아바타 모션 스튜디오 & 가이드 모드 | **완료** | **[NEW]** 반복 재생(Shadowing) 및 단어 탐색기 |
| **13단계** | UI/UX 클린 뷰(Clean View) 적용 | **완료** | **[NEW]** 인식 오버레이 제거 및 다중 창 동기화 |

---

## 3. 핵심 기술 개발 상세 (2026.05.12 업데이트)

### 3.1. 인식 엔진 정밀도 최적화 (`api/core/ml_utils.py`, `api/services/knn_classifier.py`)
- **특징 벡터 정규화 (0~1 Scaling)**: 관절 각도와 거리 데이터의 단위를 통일하여 특정 요소에 치우치지 않는 균형 잡힌 인식 환경 구축.
- **코사인 유사도 (Cosine Similarity) 도입**: 유클리드 거리 방식의 한계를 극복하고, 동작의 '절대 좌표'보다 **'형태와 방향성'**을 중시하는 엔진으로 업그레이드.
- **데이터 증강(Augmentation) 강화**: 회전 범위를 ±20도로 확대하고 스케일 변동폭을 넓혀 다양한 카메라 각도 및 사용자 체형에 대응.

### 3.2. 아바타 모션 스튜디오 (`frontend/avatar_only.html`)
- **멀티 윈도우 동기화**: 웹소켓 브로드캐스트를 통해 인식 결과가 메인 창과 아바타 전용 창에 동시에 실시간 반영되도록 설계.
- **단어 탐색기 사이드바**: KNN에 학습된 전체 단어 목록을 실시간으로 가져와 사용자가 직접 선택하고 재생할 수 있는 탐색 환경 제공.
- **동작 가이드 (Guide/Loop Mode)**: 동작을 무한 반복 재생하여 사용자가 아바타의 정석 동작을 따라하며 연습할 수 있는 Shadowing 모드 구현.

### 3.3. UI/UX 클린 뷰 및 데이터 무결성 개선
- **오버레이 제거**: 카메라와 아바타 중앙을 가리던 커다란 예측 결과 말풍선을 제거하여 동작 관찰의 편의성 증대.
- **복합어 처리 로직**: `이론,쓰다` 등 콤마(,)가 포함된 복합 단어 라벨링의 인코딩 및 파일명 불일치 문제 해결.
- **전역 재학습 파이프라인**: 개선된 알고리즘을 전체 데이터셋에 즉시 적용하는 자동 재학습 스크립트(`retrain_global.py`) 구축.

---

## 4. 시스템 아키텍처 (2026.05.12 갱신)

```text
┌─ Frontend (port 3000) ─────────────────────────────────┐
│  index.html / avatar_only.html                          │
│  ├─ avatar.js          : Three.js 고정밀 렌더링          │
│  ├─ motion_loader_npy  : [NEW] 가이드 모드(Loop) 지원    │
│  ├─ vision.js          : MP Vision + 실시간 특징 추출    │
└────────────────────────────────────────────────────────┘
                         │
                  WebSocket (8000)
                         │
┌─ Backend (port 8000) ─────────────────────────────────┐
│  api/main.py                                           │
│  ├─ services/knn_classifier: [NEW] 코사인 유사도 엔진    │
│  ├─ services/motion_extractor: 복합어 처리 로직 강화     │
│  ├─ ml/ml_utils.py     : [NEW] 0~1 정규화 특징 추출      │
│  └─ knn_model.pkl      : [NEW] 정교화된 50개 단어 모델   │
└────────────────────────────────────────────────────────┘
```

---

## 5. 🚨 향후 과제 (Remaining Tasks)

1. **문장 연결(Motion Chaining)**: 개별 단어 동작 사이의 부드러운 연결(Transition) 애니메이션 생성 엔진 연구.
2. **다국어 확장**: 한국수어(KSL) 외에 국제수어(IS) 또는 타국 수어 데이터셋의 학습 파이프라인 확장성 검증.
3. **사용자 적응형 KNN**: 사용자별로 특정 동작의 인식이 잘 안 될 경우, 해당 사용자의 데이터를 즉시 추가 학습하는 On-device 미세 조정 기능 연구.

---

## 6. 결론

Project ITDA는 3D 아바타 렌더링 기술을 넘어, **사용자가 아바타의 동작을 직접 따라하고 교정받을 수 있는 양방향 학습 환경**을 완성했습니다. 특히 2026.05.12 업데이트를 통해 인식 엔진의 수학적 모델을 고도화함으로써, 전문가 수준의 정밀한 수어 인식 및 실시간 가이드가 가능한 완성도 높은 플랫폼으로 진화하였습니다.

**작성자**: 개발팀 리드 반디 (Antigravity)
**통합 날짜**: 2026.05.12

---

## 🧭 문서 이력
- 2026.04.15: 프로젝트 초기 아키텍처 설계
- 2026.04.29: 3D 골격 렌더링 최적화 및 UX 개편
- 2026.05.08: 양손 3D 인식 엔진 및 자동 학습 파이프라인 구축
- **2026.05.12 (최종 통합): 인식 엔진 정밀도 고도화(코사인 유사도), 아바타 가이드 시스템 및 클린 뷰 UI 적용**


---

## 📅 진행 날짜: 2026.05.13

# 🤝 Project ITDA(잇다): AI 기반 수어 번역 및 교육 플랫폼
## [개발 진행 보고서] — 2026.05.13 최종 업데이트 (KSL 데이터셋 자동화 및 인식 엔진 고도화)

본 보고서는 2026.05.12 보고서의 인식 엔진 정밀화 성과를 바탕으로, **KSL 제스처 데이터셋 파이프라인 자동화 및 고정밀 3D 모션 데이터 통합(2026.05.13)** 성과를 통합하여 작성된 문서입니다.

---

## 1. 프로젝트 개요 (Project Overview)
- **프로젝트 명**: ITDA (잇다)
- **핵심 목표**: 청각장애인과 비장애인의 원활한 소통을 위해 AI(SLM/RAG/Vision) 기술을 활용한 실시간 수어 번역 및 교육용 아바타 플랫폼 구축.
- **최상위 설계 원칙**: **"수화 캐릭터가 수화를 정확히 표현하고, 시스템이 사용자의 수어를 정확히 인식하는 것."** 단순한 동작 흉내를 넘어, 해부학적 정확도와 표준 수형 규격(KSL)을 준수하는 고정밀 양방향 통번역 환경 구현.

---

## 2. 전체 개발 진행률 (Overall Progress)

기존 인식 엔진에 고정밀 학습 데이터를 대량 공급하고, 실시간 추적 안정성을 확보하여 상용 수준의 엔진 완성도 달성.

| 단계 | 개발 항목 | 진행 상태 | 비고 |
| :--- | :--- | :---: | :--- |
| **1-13단계** | 아키텍처 및 기본 UI 통합 | 완료 | V5 통합 완료 |
| **14단계** | KSL 제스처 데이터셋 자동화 파이프라인 | **완료** | **[NEW]** 20개 핵심 단어 랜드마크 추출 및 자동 검증 |
| **15단계** | 인식 엔진 피처 엔지니어링 고도화 | **완료** | **[NEW]** 정규화된 기하학적 벡터(Normalized Geometric Vectors) |
| **16단계** | 실시간 추적 안정성 강화 | **완료** | **[NEW]** MediaPipe 신뢰도 임계값 최적화 및 추적 연속성 확보 |

---

## 3. 핵심 기술 개발 상세 (2026.05.13 업데이트)

### 3.1. KSL 데이터셋 파이프라인 자동화 (`api/services/motion_extractor.py`, `api/services/data_pipeline.py`)
- **20개 핵심 단어 데이터셋 구축**: 교육용 핵심 어휘 20종에 대해 고품질 랜드마크 데이터를 추출하고 수동 검수 단계를 자동화함.
- **고정밀 3D 모션(JSON) 통합**: 아바타 피드백을 위해 랜드마크 데이터를 Three.js 호환 JSON 포맷으로 변환하여 프론트엔드(`frontend/data/ksl_motions/`)에 즉시 공급하는 체계 구축.
- **자동 검증(Verification) 로직**: 추출된 데이터의 프레임 무결성 및 유효성을 체크하여 학습 데이터의 품질을 보장.

### 3.2. 인식 엔진 피처 엔지니어링 고도화 (`api/core/ml_utils.py`)
- **정규화된 기하학적 벡터(Normalized Geometric Vectors)**: 단순 좌표값이 아닌, 관절 간의 상대적 각도와 거리를 정규화하여 사용자 체형과 카메라 거리 변화에도 강건한 인식 성능 확보.
- **KNN 50단어 모델 최적화**: 고품질 데이터셋을 활용하여 KNN 모델을 재학습함으로써, 유사 동작 간의 혼동을 최소화하고 실시간 추론 정확도를 대폭 개선.

### 3.3. 실시간 추적 및 예측 인터페이스 안정화 (`frontend/js/vision.js`)
- **MediaPipe 신뢰도 최적화**: 랜드마크 추적 임계값(Confidence Threshold)을 미세 조정하여 빠른 손동작에서도 스켈레톤이 끊기지 않고 연속적으로 유지되도록 개선.
- **데이터 흐름 최적화**: 프론트엔드와 백엔드 간의 특징 벡터 전송 효율을 높여 레이턴시를 최소화함.

---

## 4. 시스템 아키텍처 (2026.05.13 갱신)

```text
┌─ Frontend (port 3000) ─────────────────────────────────┐
│  index.html / avatar_only.html                          │
│  ├─ avatar.js          : Three.js 고정밀 렌더링          │
│  ├─ motion_loader_json : [NEW] 고정밀 JSON 모션 로딩     │
│  └─ vision.js          : [NEW] MP Vision 임계값 최적화   │
└────────────────────────────────────────────────────────┘
                         │
                  WebSocket (8000)
                         │
┌─ Backend (port 8000) ─────────────────────────────────┐
│  api/main.py                                           │
│  ├─ services/knn_classifier: [NEW] 50단어 최적화 모델    │
│  ├─ services/data_pipeline : [NEW] 자동화 파이프라인     │
│  └─ ml/ml_utils.py     : [NEW] 기하학적 벡터 정규화      │
└────────────────────────────────────────────────────────┘
```

---

## 5. 🚨 향후 과제 (Remaining Tasks)

1. **에지 케이스 수집**: 조명 조건이 불량하거나 손이 겹치는(Occlusion) 상황에서의 인식 복원력 강화.
2. **모바일 최적화**: 웹 브라우저 기반 실시간 추적의 연산 부하를 줄여 모바일 환경에서도 원활한 구동 보장.
3. **피드백 UI 고도화**: 사용자의 동작이 아바타와 얼마나 일치하는지 시각적인 점수(Score) 또는 히트맵으로 보여주는 기능.

---

## 6. 결론

Project ITDA는 2026.05.13 업데이트를 통해 **데이터 수집-학습-인식-표현**으로 이어지는 전체 파이프라인의 자동화와 고도화를 달성했습니다. 특히 정규화된 기하학적 벡터 도입과 데이터셋 검증 자동화는 향후 수천 단어 이상의 대규모 수어 사전 구축을 위한 기술적 토대를 마련한 중요한 성과입니다.

**작성자**: 개발팀 리드 반디 (Antigravity)
**통합 날짜**: 2026.05.13

---

## 🧭 문서 이력
- 2026.04.15: 프로젝트 초기 아키텍처 설계
- 2026.05.08: 양손 3D 인식 엔진 및 자동 학습 파이프라인 구축
- 2026.05.12: 인식 엔진 정밀도 고도화(코사인 유사도), 아바타 가이드 시스템 적용
- **2026.05.13 (최종 통합): KSL 데이터셋 파이프라인 자동화, 피처 엔지니어링 고도화 및 실시간 추적 안정화**


---

# 📅 진행 날짜: 2026.05.14

### ✅ 핵심 업데이트: 인식 엔진 고도화 및 안정성 강화
오늘은 KSL 인식의 '정확도'와 '구분력'을 실사용 수준으로 끌어올리기 위한 전면적인 엔진 업그레이드를 진행했습니다.

#### 1. 시계열 모션 특징(Velocity) 도입 (76차원 확장)
- **궤적 분석**: 단순히 손 모양만 보는 것이 아니라, 손이 움직이는 방향(dx, dy, dz)을 계산하여 '가다/오다' 같은 반대 동작을 완벽히 구분하도록 개선했습니다.
- **공간 감도 극대화**: 코(Nose) 기준의 Y축(높이)과 X축(좌우) 가중치를 3배로 높여 '나/너'(가슴)와 '맛있다/좋다'(얼굴)의 혼동을 해결했습니다.

#### 2. 데이터셋 품질 혁신 (Data Trimming)
- **자동 자르기 로직**: 사전 영상 학습 시 앞뒤의 불필요한 '정지 프레임'을 20~35% 제거하여 순수 동작 데이터만 추출합니다.
- **'친구' 도배 해결**: 오인식이 많았던 '친구' 단어의 추출 구간을 더 엄격하게 제한하여 시스템 전체의 안정성을 확보했습니다.

#### 3. 장애 조치 및 성능 최적화
- **Gemini API 예외 처리**: 403 인증 에러 발생 시 10분간 자동 차단 및 로컬 엔진(Ollama/Rule-based)으로 즉시 전환하는 서킷 브레이커를 구현했습니다.
- **GPU 가속 및 60fps 지원**: MediaPipe 설정을 최적화하여 저사양 환경에서도 실시간 인식이 가능하도록 성능을 튜닝했습니다.
- **인식 문턱값 상향 (0.6)**: 60% 이상의 확신이 들 때만 결과를 출력하여 '아무 동작도 안 할 때' 오작동하는 현상을 방지했습니다.

---


---

# 📅 진행 날짜: 2026.05.20

# 🤝 Project ITDA(잇다): AI 기반 수어 번역 및 교육 플랫폼
## [개발 진행 보고서] — 2026.05.20 최종 업데이트 (웹 제어 고도화 및 대화 전용 수어 데이터 완전 자동 빌드)

본 보고서는 2026.05.14 보고서의 인식 엔진 안정화 성과를 바탕으로, **웹 UI 실시간 제어 기능 고도화(카메라 ON/OFF, 모델 핫스왑, 크로스 탭 연동) 및 대화 전용 80단어 수어 데이터 완전 자동 빌드** 성과를 통합하여 작성된 문서입니다.

---

## 1. 프로젝트 개요 (Project Overview)
- **프로젝트 명**: ITDA (잇다)
- **핵심 목표**: 청각장애인과 비장애인의 원활한 소통을 위해 AI(SLM/RAG/Vision) 기술을 활용한 실시간 수어 번역 및 교육용 아바타 플랫폼 구축.
- **최상위 설계 원칙**: **"수화 캐릭터가 수화를 정확히 표현하고, 시스템이 사용자의 수어를 정확히 인식하는 것."** 단순한 동작 흉내를 넘어, 해부학적 정확도와 표준 수형 규격(KSL)을 준수하는 고정밀 양방향 통번역 환경 구현.

---

## 2. 전체 개발 진행률 (Overall Progress)

| 단계 | 개발 항목 | 진행 상태 | 비고 |
| :--- | :--- | :---: | :--- |
| **1-16단계** | 아키텍처 및 인식 엔진 통합 | 완료 | V6 통합 완료 |
| **17단계** | 웹캠 실시간 ON/OFF 제어 | **완료** | **[NEW]** 네온 글로우 토글 버튼 및 트랙 정지 |
| **18단계** | KNN 모델 실시간 핫스왑 (Web UI) | **완료** | **[NEW]** 서버 재기동 없는 인메모리 모델 교체 |
| **19단계** | 메인 창 ↔ 아바타 뷰 모델 동기화 | **완료** | **[NEW]** BroadcastChannel 크로스 탭 연동 |
| **20단계** | 대화 전용 80단어 모델 수어 데이터 자동 빌드 | **완료** | **[NEW]** 동의어 매핑 + 일괄 추출 (실패 0건) |

---

## 3. 핵심 기술 개발 상세 (2026.05.20 업데이트)

### 3.1. 웹캠 실시간 ON/OFF 제어 (`frontend/js/vision.js`, `frontend/index.html`)
- **공개 API 추가**: `start()`, `stop()`, `isRunning()` 메서드를 `ITDAVision5` 객체에 수립하여 모델 리로드 없이 카메라 장치를 온디맨드로 정지·재활성화.
- **프리미엄 토글 버튼**: 카메라 뷰포트 좌상단에 시안 블루 / 레드 네온 글로우 스타일의 `카메라: ON/OFF` 버튼 탑재.

### 3.2. KNN 모델 실시간 핫스왑 (`api/services/knn_classifier.py`, `api/routers/collect.py`)
- **인메모리 핫스왑 구조**: `set_model_type()` 호출 시 전역 캐시를 무효화하고 `reload_model()`을 통해 새 모델을 즉시 바인딩.
- **Web UI 드롭다운 연동**: 헤더의 `<select id="sel-model">` 변경 이벤트 → `/api/collect/select-model` API 호출 → 서버 재기동 없이 기본 모델(16단어) ↔ 대화 모델(80단어) 전환.

### 3.3. 크로스 탭 모델 동기화 (`frontend/avatar_only.html`)
- **BroadcastChannel API**: `itda_model_channel`을 통해 메인 창의 모델 변경 이벤트를 아바타 전용 뷰에 실시간 전파.
- **동적 단어 사이드바 갱신**: 이벤트 수신 시 `/api/sign-language/list-motions`를 재호출하여 현재 모델에 맞는 단어 목록(16개 ↔ 80개)으로 사이드바 즉시 재렌더링.

### 3.4. 대화 전용 80단어 수어 데이터 완전 자동 빌드 (`api/services/motion_extractor.py`, `scratch/pre_extract_motions.py`)
- **동의어 매핑 통합 (`DIALOGUE_WORDS_MAP`)**: 대화 모델 80단어와 국립국어원 한국수어사전 원본 키 간의 매핑 딕셔너리를 `MotionExtractor`에 직접 이식. `"농어"` → `"농인,농아인"`, `"서툴다"` → `"미숙하다"` 등 14종 유의어 경로를 자동 탐색.
- **로딩 UX 고도화**: 아바타 뷰 사이드바에 ⏳ 회전 스피너 + "생성 중..." 애니메이션 탑재. `try…finally` 블록으로 네트워크 오류 시에도 버튼 텍스트 안전 복원.
- **일괄 자동 빌드 결과**: `pre_extract_motions.py` 스크립트로 80단어를 일괄 처리.
  - ⏭️ 스킵 (이미 존재): **34개**
  - ✅ 신규 추출 성공: **46개**
  - ❌ 실패: **0건**

---

## 4. 시스템 아키텍처 (2026.05.20 갱신)

```text
┌─ Frontend (port 3000) ─────────────────────────────────┐
│  index.html / avatar_only.html                          │
│  ├─ vision.js     : [NEW] start/stop/isRunning API      │
│  ├─ BroadcastChannel('itda_model_channel')              │
│  │    └─ 메인 창 모델 변경 → 아바타 뷰 실시간 동기화     │
│  └─ 사이드바 단어 목록 : ⏳ 로딩 인디케이터 탑재         │
└────────────────────────────────────────────────────────┘
                         │
                  WebSocket (8000)
                         │
┌─ Backend (port 8000) ─────────────────────────────────┐
│  api/main.py                                           │
│  ├─ knn_classifier : [NEW] 인메모리 핫스왑 엔진         │
│  ├─ collect router : [NEW] /select-model 엔드포인트    │
│  ├─ sign_language router : list-motions 동적 쿼리       │
│  └─ motion_extractor : [NEW] DIALOGUE_WORDS_MAP 통합   │
└────────────────────────────────────────────────────────┘
```

---

## 5. 🚨 향후 과제 (Remaining Tasks)

1. **문장 연결(Motion Chaining)**: 개별 단어 동작 사이의 부드러운 Transition 애니메이션 생성 엔진 연구.
2. **사용자 적응형 KNN**: 사용자별 On-device 미세 조정으로 개인화된 인식 정확도 향상.
3. **모바일 최적화**: 웹 브라우저 실시간 추적의 연산 부하를 줄여 모바일 환경 원활한 구동 보장.

---

## 6. 결론

Project ITDA는 2026.05.20 업데이트를 통해 **웹 UI에서 카메라와 AI 모델을 실시간으로 제어하는 완전한 관리 콘솔**을 완성하고, 대화 전용 80단어 전체에 대한 수어 3D 랜드마크 데이터를 자동으로 수집·빌드하는 **무한 확장형 데이터 파이프라인**을 가동했습니다. 이로써 ITDA는 단순 번역 도구를 넘어, 실시간 양방향 수어 교육 및 대화 보조 플랫폼으로서의 완성도를 갖추었습니다.

**작성자**: 개발팀 리드 반디 (Antigravity)
**통합 날짜**: 2026.05.20

---

## 🧭 문서 이력
- 2026.04.15: 프로젝트 초기 아키텍처 설계
- 2026.04.29: 3D 골격 렌더링 최적화 및 UX 개편
- 2026.05.08: 양손 3D 인식 엔진 및 자동 학습 파이프라인 구축
- 2026.05.12: 인식 엔진 정밀도 고도화(코사인 유사도), 아바타 가이드 시스템 적용
- 2026.05.13: KSL 데이터셋 파이프라인 자동화, 피처 엔지니어링 고도화
- 2026.05.14: 시계열 모션 특징(Velocity) 도입, Data Trimming, 서킷 브레이커 구현
- 2026.05.20: 웹캠 ON/OFF 제어, KNN 핫스왑, 크로스 탭 동기화, 대화 80단어 수어 데이터 완전 자동 빌드


---

# 📅 진행 날짜: 2026.05.22

# 🤝 Project ITDA(잇다): AI 기반 수어 번역 및 교육 플랫폼
## [개발 진행 보고서] — 2026.05.22 업데이트 (전면 코드 품질 검토, 정직한 평가 체계 구축, 데이터 출처 추적)

본 보고서는 2026.05.20 보고서의 웹 제어·대화 데이터 자동 빌드 성과를 바탕으로, **전체 코드베이스 품질 검토 및 Critical 버그 수정, 데이터 누수 없는 정직한 정확도 평가 체계 구축, 출처(source) 기반 데이터 추적 및 멀티-take 수집 환경** 작업을 정리한 문서입니다. 오늘 작업은 신규 기능보다 **엔지니어링 신뢰성 확보와 실측 기반 현황 파악**에 집중되었습니다.

---

## 2. 전체 개발 진행률 (Overall Progress)

| 단계 | 개발 항목 | 진행 상태 | 비고 |
| :--- | :--- | :---: | :--- |
| **1-20단계** | 인식·아바타·웹 제어 통합 | 완료 | V6 통합 완료 |
| **21단계** | 전면 코드 품질 검토 및 Critical 버그 수정 | **완료** | **[NEW]** 이벤트 루프 블로킹·SSRF·경로 탈출·자원 누수 해소 |
| **22단계** | 누수 없는 정직한 정확도 평가 체계 | **완료** | **[NEW]** GroupKFold 기반 evaluate_knn.py, 실측 정확도 확보 |
| **23단계** | 데이터 출처(source) 추적 도입 | **완료** | **[NEW]** 수집 경로별 출처 기록, CSV 스키마 확장 |
| **24단계** | 멀티-take 수집 UI 및 모델별 분리 | **완료** | **[NEW]** 단어당 다회 녹화, main/dialogue 데이터셋 분리 |
| **25단계** | 죽은 코드 제거 및 스크립트 이식성 개선 | **완료** | **[NEW]** 미사용 모듈 제거, argparse 전환 |

---

## 3. 핵심 기술 개발 상세 (2026.05.22 업데이트)

### 3.1. 전면 코드 품질 검토 및 Critical 버그 수정
전체 코드베이스(약 40개 소스 파일)를 검토하여 런타임 안정성·보안 결함을 수정.
- **이벤트 루프 블로킹 해소**: `/api/collect/video` 등 CPU 집약 작업을 `anyio.to_thread`로 분리하여 영상 분석 중 다른 요청·WebSocket이 멈추던 문제 제거.
- **보안**: 영상 URL 수집 엔드포인트에 신뢰 도메인 허용목록을 적용해 SSRF를 차단하고, 업로드 파일명을 UUID로 대체해 경로 탈출(path traversal)을 방지.
- **자원 누수 차단**: 임시 영상 파일을 `try/finally`로 항상 정리.
- **기동 안정화**: deprecated된 `@app.on_event`를 `lifespan`으로 전환, 파이프라인 빌드를 스레드로 분리하고 실패해도 서버가 기동되도록 예외 처리.
- **지연 초기화**: VectorDB 임베딩 모델을 import 시점이 아닌 최초 사용 시점에 로드하도록 변경.
- **스레드 안전성**: KNN 모델 로드/교체에 Lock을 적용.
- **속도 특징 오염 버그 수정**: 좌우 스왑 보조 추론이 속도 계산용 전역 상태를 오염시키던 문제를 snapshot/restore로 격리.
- **프론트엔드 안정화**: WebSocket이 3회 실패 후 영구 정지하던 것을 무한 백오프 재연결로 개선, 카메라·네트워크 오류 처리 보강, 백엔드 호스트 표기를 `location.hostname` 기반으로 통일(localhost/127.0.0.1 혼선 제거).

### 3.2. 누수 없는 정직한 정확도 평가 체계 (`api/scripts/evaluate_knn.py`)
- **데이터 누수 인지 평가**: 같은 출처(영상·세션)에서 나온 샘플이 학습·검증 양쪽에 섞이면 정확도가 부풀려지는 문제를 인지하고, 출처 단위로 분리하는 GroupKFold 평가를 도입.
- **실측 결과**: 현행 모델의 교차검증 정확도는 **기본 모델(16단어) 약 52.5%, 대화 모델(80단어) 약 16.5%**로 측정됨. 기존의 정성적 서술을 실제 수치로 교정.
- **시사점**: 인식 성능의 병목은 알고리즘이 아니라 **학습 데이터의 다양성 부족**(단어당 사실상 단일 수어자)임을 데이터로 확인.

### 3.3. 데이터 출처(source) 추적 및 학습 파이프라인 정비 (`api/routers/collect.py`)
- **출처 기록**: 라이브 수집 세션·영상 파일·URL 등 모든 수집 경로가 고유 `source`를 기록하도록 변경. `ksl_dataset.csv` / `ksl_dataset_dialogue.csv` 스키마에 `source` 열 추가.
- **학습 데이터 손실 버그 수정**: 훈련 시 `head(100)`이 오래된 데이터만 남기고 새로 수집한 샘플을 버린 뒤 CSV를 덮어써 데이터를 영구 삭제하던 문제를 수정. 이제 CSV 원본은 보존하고, 균형 조정은 학습 메모리 상에서 무작위 추출로 수행.
- **모델별 데이터셋 분리**: 선택된 모델(main/dialogue)에 따라 수집·훈련 대상 CSV·모델 파일이 자동으로 분리되도록 수정.

### 3.4. 멀티-take 수집 UI 재구성 (`frontend/js/collect_ui.js`, `frontend/index.html`)
- **다회 녹화 흐름**: 한 단어를 같은 동작으로 여러 번(take) 녹화하는 UI로 재구성. 녹화 1회 = 출처 1개로, take가 쌓일수록 데이터 다양성이 커지고 누수 없는 평가가 가능.
- **UI 정리**: 모드 전환 버튼(번역/학습/수집)을 상단 우측으로 이동하고 패널 표시/숨김 토글을 추가. 3D 모델 미사용으로 무의미해진 아바타/스켈레톤 전환 버튼을 통합 제거.

### 3.5. 죽은 코드 제거 및 스크립트 이식성 개선
- **미사용 코드 제거**: 동작하지 않는 STT 어댑터 경로, SLM 에이전트의 미사용 추론 함수, avatar.js의 미사용 3D 모델 로딩 코드를 제거.
- **스크립트 이식성**: 사용자별 하드코딩 절대경로(`C:\Users\...`)를 쓰던 스크립트들을 argparse 기반으로 전환하여 다른 환경에서도 실행 가능하게 함.
- **저장소 정리**: 추적되던 `__pycache__` 및 실험용 `scratch/`를 git 추적에서 제외.

---

## 4. 외부 데이터셋 검토 결과 (AI Hub)

대규모 학습 데이터 확보를 위해 AI Hub 한국수어 데이터셋의 활용을 검토함.
- **결과**: AI Hub가 제공하는 keypoint 데이터는 **OpenPose 형식**으로, 본 프로젝트의 **MediaPipe 기반 파이프라인과 호환되지 않음**을 실측으로 확인(변환 샘플의 최근접 이웃 일치율 0%).
- **시사점**: AI Hub 데이터를 쓰려면 원천 영상(파일당 20~36GB)을 받아 MediaPipe로 직접 추출해야 하며, keypoint 직접 변환 경로는 사용 불가.

---

## 5. 🚨 향후 과제 (Remaining Tasks)

1. **인식 정확도 개선 — 데이터 다양성 확보**: 현재 병목은 단어당 단일 수어자. 멀티-take 수집을 여러 사람이 수행하여 실제 다양성을 확보하는 것이 최우선.
2. **단어 수 조정**: 대화 모델 80단어는 현 데이터로 실용 정확도 미달. 핵심 단어 위주로 범위를 좁혀 단계적으로 확장.
3. **시퀀스 모델 검토**: 프레임 단위 KNN은 본질적으로 움직임(시퀀스)인 수어 인식에 한계가 있음. 데이터 확보 후 LSTM 등 시퀀스 모델 도입 검토.

---

## 6. 결론

2026.05.22 작업은 신규 기능 추가보다 **코드베이스의 신뢰성 확보와 현황의 정직한 측정**에 집중되었습니다. 다수의 런타임·보안 결함을 수정하고, 데이터 누수 없는 평가 체계를 통해 인식 정확도의 실제 수치를 확인했으며, 출처 기반 데이터 추적과 멀티-take 수집 환경을 구축했습니다. 이 작업들은 화려한 진전은 아니지만, 향후 데이터 다양성 확보와 인식 성능 개선을 위한 **정확한 출발점과 견고한 토대**를 마련했습니다.

**작성자**: 개발팀
**통합 날짜**: 2026.05.22

---

## 🧭 문서 이력
- 2026.04.15: 프로젝트 초기 아키텍처 설계
- 2026.04.29: 3D 골격 렌더링 최적화 및 UX 개편
- 2026.05.08: 양손 3D 인식 엔진 및 자동 학습 파이프라인 구축
- 2026.05.12: 인식 엔진 정밀도 고도화(코사인 유사도), 아바타 가이드 시스템 적용
- 2026.05.13: KSL 데이터셋 파이프라인 자동화, 피처 엔지니어링 고도화
- 2026.05.14: 시계열 모션 특징(Velocity) 도입, Data Trimming, 서킷 브레이커 구현
- 2026.05.20: 웹캠 ON/OFF 제어, KNN 핫스왑, 크로스 탭 동기화, 대화 80단어 수어 데이터 완전 자동 빌드
- 2026.05.22: 전면 코드 품질 검토 및 Critical 버그 수정, 누수 없는 정직한 평가 체계, 출처 추적 및 멀티-take 수집 환경 구축
- **2026.05.27 (최신): KNN 모델 재학습 복구, 60fps 덮어쓰기 버그 수정, UI 전면 단순화(탭/카드/잉여 토글 제거 → 햄버거 메뉴 통합), 통역 이력 영속화 및 버퍼 누적 표시 구현**


---


# 📅 진행 날짜: 2026.05.27

# 🤝 Project ITDA(잇다): AI 기반 수어 번역 및 교육 플랫폼
## [개발 진행 보고서] — 2026.05.27 업데이트 (인식 복구 및 UI 전면 단순화·이력 영속화)

본 보고서는 2026.05.22 보고서의 평가 체계·데이터 추적 작업을 바탕으로, **운영 중 발생한 인식 불능 장애 복구, 60fps 브로드캐스트로 인한 UI 가시성 버그 수정, 메인 창 UI 전면 단순화(탭/카드 → 햄버거 메뉴 통합), 통역 이력 패널 재구현(영속화·버퍼 누적·문장 확정)** 작업을 정리한 문서입니다.

---

## 1. 프로젝트 개요 (Project Overview)
- **프로젝트 명**: ITDA (잇다)
- **핵심 목표**: 청각장애인과 비장애인의 원활한 소통을 위해 AI(SLM/RAG/Vision) 기술을 활용한 실시간 수어 번역 및 교육용 아바타 플랫폼 구축.
- **최상위 설계 원칙**: **"수화 캐릭터가 수화를 정확히 표현하고, 시스템이 사용자의 수어를 정확히 인식하는 것."** 단순한 동작 흉내를 넘어, 해부학적 정확도와 표준 수형 규격(KSL)을 준수하는 고정밀 양방향 통번역 환경 구현.

---

## 2. 전체 개발 진행률 (Overall Progress)

| 단계 | 개발 항목 | 진행 상태 | 비고 |
| :--- | :--- | :---: | :--- |
| **1-25단계** | 인식·평가·데이터 추적 통합 | 완료 | V7 통합 완료 |
| **26단계** | KNN 모델 재학습 복구 (main / dialogue) | **완료** | **[NEW]** 누락된 `.pkl` 두 모델 모두 재훈련 |
| **27단계** | 60fps 브로드캐스트 가시성 버그 수정 | **완료** | **[NEW]** 성공 인식 후 3.5초 보호 윈도우 |
| **28단계** | 메인 창 UI 전면 단순화 | **완료** | **[NEW]** 탭/카드/잉여 토글 제거, 학습·수집은 새창에만 |
| **29단계** | 헤더 햄버거 메뉴 통합 | **완료** | **[NEW]** 카메라/음성/새창/모델 4개 컨트롤 일원화 |
| **30단계** | 로고·헤더 상태 표시 컴팩트화 | **완료** | **[NEW]** dot+짧은 라벨 status-pill 디자인 |
| **31단계** | 통역 이력 패널 영속화 및 누적 구현 | **완료** | **[NEW]** localStorage + 버퍼 누적 + 문장(💬) 확정 |

---

## 3. 핵심 기술 개발 상세 (2026.05.27 업데이트)

### 3.1. KNN 모델 재학습 복구 (`api/services/knn_classifier.py`, `api/routers/collect.py`)
운영 중 백엔드는 정상 기동하지만 수어 동작이 **모두 "미인식"으로 처리되는 장애**가 발생함.
- **원인 진단**: `api/data/ksl_training/` 디렉터리에 학습된 `knn_model.pkl` / `knn_model_dialogue.pkl` 두 모델 파일이 모두 부재. `knn_classifier.is_model_ready()`가 `False`를 반환하면서 [ws_vision.py:253](api/routers/ws_vision.py#L253)의 예측 블록 전체가 스킵되고 곧바로 "KNN 신뢰도 미달" 분기로 빠지던 상황.
- **CSV 데이터는 무사**: `ksl_dataset.csv`(1,601행), `ksl_dataset_dialogue.csv`(11,195행) 모두 보존되어 있었으므로 학습만 다시 실행하면 복구 가능.
- **HTTP API 기반 재학습**: 서버 재기동 없이 `/api/collect/select-model` → `/api/collect/train` 호출 시퀀스로 두 모델 모두 재훈련.
  - **main 모델**: 16단어 / 1,600 샘플 / CV 정확도 **47.9%**
  - **dialogue 모델**: 80단어 / 11,142 샘플 / CV 정확도 **34.0%**
- **결과**: `reload_model()`이 학습 직후 자동 호출되어 메모리에 새 모델이 즉시 바인딩됨. 다시 정상 예측 동작.
- **재발 방지 메모**: `.pkl`은 `.gitignore` 대상이라 브랜치 전환·clean 작업 시 손실 위험. 학습 직후 별도 백업 권장.

### 3.2. 60fps 브로드캐스트로 인한 가시성 버그 수정 (`frontend/index.html`)
새창(`avatar_only.html`)에서는 인식된 단어가 화면에 잘 보이는데, 메인 창에서는 같은 동작을 해도 **"🤔 미확정: 다시 보여주세요"만 뜨고 인식 단어가 보이지 않는 현상**.
- **원인 분석**: 백엔드 `manager.send_ack()`는 모든 연결된 WS 클라이언트에 브로드캐스트함. 카메라가 어느 한 창에서만 켜져 있어도, 다른 창의 WS는 모든 ACK(성공/실패)를 수신함. 카메라 프레임이 60fps로 들어오기 때문에:
  1. KNN 성공 1회 → `"안녕하세요"` 1회 브로드캐스트
  2. 다음 프레임들 KNN 실패 → `"미인식"` 수십 회 연속 브로드캐스트
  3. 결과적으로 메인 창의 `slm-output`이 성공 후 **16ms 만에 "미확정"으로 덮어써져** 사람 눈에 안 보임.
- **새창은 멀쩡했던 이유**: [avatar_only.html:508](frontend/avatar_only.html#L508)에 `"대기 중"` / `"미인식"` 메시지를 명시적으로 무시하는 가드가 있었음.
- **해결**: 메인 창 핸들러에 **3.5초 보호 윈도우(`SUCCESS_HOLD_MS`)** 도입.
  - 성공 인식 시 `lastSuccessAt = Date.now()` 기록
  - 그 후 3.5초 동안 들어오는 `"대기 중"` / `"미인식"` / `"None"` 응답은 UI 덮어쓰기 무시
  - 3.5초 경과 후 자연스럽게 "미확정" 표시로 복귀
- **결과**: 메인 창도 새창처럼 성공 인식이 사람 눈에 명확히 보이게 됨. 동일 데이터 흐름이지만 UI 레이어의 디바운싱으로 해결.

### 3.3. 메인 창 UI 전면 단순화 — 잉여 컨트롤 제거 (`frontend/index.html`)
새창(아바타 뷰)에 학습·수집 카드가 이미 구현되어 있어, 메인 창의 동일 기능 중복을 정리.
- **제거 1차 — 모드 탭 + 패널**:
  - `#mode-toggle` (번역/학습/수집 3개 탭 버튼)
  - `#lesson-card` (학습 카드 패널, 단어 가이드 + prev/next/stop 네비)
  - `#collect-card` (수집 카드 패널, 멀티-take 녹화 + URL/파일 학습)
  - 관련 JS: `_setMode` / `_activateWord` / `_applyPanelVisibility` / `modeBtns` / `itda:lesson:feedback` 핸들러
  - 관련 CSS: `.mode-btn`, `.lesson-*`, `.collect-*`, `.btn-collect*`, `@keyframes lesson-success-pulse` (약 90줄)
- **제거 2차 — UI 토글 버튼**: 학습/수집 카드가 사라져 더 이상 토글할 패널이 없게 된 `#btn-ui-toggle` (☰) 제거.
- **유지**: 하단 `#input-section`(텍스트→수어 번역 + STT 마이크), translator.js / STT 스크립트 — 새창에 없는 기능이므로 보존.
- **부수 효과**: `education.js` / `collect_ui.js` 스크립트는 옵셔널 체이닝으로 DOM 부재를 안전 처리하므로 import는 유지(새창에서 동일 모듈 재사용).

### 3.4. 헤더 햄버거 메뉴 통합 (`frontend/index.html`)
산재해 있던 4개 컨트롤(카메라 토글, 음성 토글, 새창 열기, 인식 모델 선택)을 헤더 우측의 단일 ☰ 드롭다운으로 일원화.
- **메뉴 구성**: `#header-menu-wrap` > `#btn-header-menu` (☰ 버튼) + `#header-menu-panel` (드롭다운)
- **포함 항목**:
  1. `📷 카메라: ON/OFF` — 카메라 좌상단에 있던 토글을 이 위치로 이전 (라벨에 이모지 prefix 추가, ON=시안, OFF=빨강)
  2. `🔇 음성: ON/OFF` — TTS 음성 출력 토글
  3. `🪟 새창 열기` — 아바타 전용 창 진입점
  4. `인식 모델` 드롭다운 — main(16단어) ↔ dialogue(80단어)
- **인터랙션**:
  - ☰ 클릭 → 열림/닫힘 토글
  - 패널 내부 클릭은 `stopPropagation`으로 보호 (드롭다운 옵션 선택해도 닫히지 않음)
  - 바깥 클릭 또는 `Esc` → 닫힘
  - `aria-expanded` / `aria-hidden` 속성도 동기화 (접근성)
- **결과**: 헤더가 시각적으로 정돈되고, 자주 안 쓰는 컨트롤이 화면 공간을 점유하지 않음.

### 3.5. 로고 정리 및 헤더 상태 표시 컴팩트화 (`frontend/index.html`, `frontend/js/avatar.js`)
- **로고 단순화**: `ITDA SIGN` → `ITDA` (SIGN 텍스트 제거, 손 아이콘만 유지)
- **상태 표시 재디자인**: 기존의 박스형 hud-item 3개(`Backend: ONLINE` / `FPS: 10` / `Joints: ACTIVE`)를 컴팩트한 **dot+짧은 라벨** pill로 교체.

| 상태 | 이전 | 이후 |
|---|---|---|
| 백엔드 | `Backend: OFFLINE` | `● OFFLINE` (red dot) |
| FPS | `FPS: 10` | `● 10 fps` (≥24 green / ≥12 yellow / <12 red 자동 색상) |
| 3D 모델 | `Joints: ACTIVE` / `3D 모델 비활성화` | `● 3D OFF` (yellow dot) |

- **새 CSS 클래스**: `.status-pill` (둥근 pill, 작은 모노 폰트) + `.status-dot{.ok|.warn|.err}` (색상 dot + glow)
- **전체 메시지는 `title` 속성으로 이동**: hover 시 툴팁으로 확인 가능 (예: `Connected to 127.0.0.1:8000`)
- **avatar.js 갱신**: `jstEl.innerHTML = '<span class="status-dot warn"></span>3D OFF'`로 변경하여 헤더 디자인 통일.

### 3.6. 통역 이력 패널 재구현 (`frontend/index.html`)
"이력 보기" 버튼이 있긴 했으나, **새로고침 시 휘발**, **클릭해도 패널이 부모 `overflow:hidden`에 잘려 안 보임**, **중복 단어 누락**, **버퍼 누적 미지원** 등 다수 결함 → 전면 재구현.

#### (a) localStorage 영속화
- `LOG_STORAGE_KEY = 'itda:translation_log'`에 최대 100건 저장
- 페이지 진입 시 `_refreshHistoryUI()` 한 번 호출 → **새로고침 후에도 이력 복원**
- 헬퍼: `_loadHistory()` / `_saveHistory()` / `_renderHistoryItem()` / `_refreshHistoryUI()` / `pushHistoryEntry()`

#### (b) 패널 클리핑 회피
- 원래 패널은 `#top-section`(overflow:hidden) 내부의 `position:absolute; top:100%`였음 → 부모 박스 밖으로 잘려나가 보이지 않던 문제.
- **해결**: 패널을 `<body>` 직속 자식으로 이동하고 `position: fixed; z-index: 2000`으로 격상. 어떤 부모의 overflow에도 잘리지 않음.
- 토글 시 `getBoundingClientRect()`로 버튼 + LIVE TRANSLATION 영역 위치 측정 → **패널 폭을 LIVE TRANSLATION과 동일하게 정렬**, 버튼 바로 아래에 띄움. 창 리사이즈·스크롤 시 자동 재계산.

#### (c) 버퍼 누적 + 문장(💬) 확정 (백엔드 SentenceBuilder 동기화)
- `_activeBufferId`(현재 누적 중인 항목의 ts 식별자) + `BUFFER_IDLE_MS = 60000` (60초)
- 동작 규칙:

| 입력 | 동작 |
|---|---|
| 단어 1개 | 새 항목 생성 → `"이름"` |
| 단어 2개째(60초 이내) | 같은 항목에 띄어쓰기로 누적 → `"이름 천천히"` |
| 단어 3개째 | 또 누적 → `"이름 천천히 잘"` |
| `💬 ...` 문장 도착 | 누적 중인 항목을 **이 문장으로 교체** + 활성 버퍼 해제 |
| 다음 단어 | 새 항목 다시 시작 |
| 60초 초과 후 단어 | stale 처리 → 새 항목 |

- 항목 데이터 구조: `{ text, words: ["w1","w2"], conf, time, ts, isSentence }`
- 표시 색상: 단어 누적은 시안 톤, 문장(💬)은 보라 톤으로 시각 구분
- 옛 항목(`words` 필드 없음)도 `entry.text.startsWith('💬')` 폴백으로 호환 처리

#### (d) UX 개선
- 버튼에 카운트 배지(`#log-count-badge`) — 닫혀 있어도 누적 개수 표시
- 패널 헤더에 N개 요약(`#log-summary`) + 지우기 + ✖ 닫기 버튼
- 빈 상태 안내문(`#log-empty-hint`): "아직 인식된 단어나 문장이 없습니다."
- 시간 표시(분:초:millis 단축)와 신뢰도(`(87%)`) 옅게 표기

---

## 4. 시스템 아키텍처 (2026.05.27 갱신)

```text
┌─ Frontend (port 3000) ─────────────────────────────────┐
│  index.html                                              │
│  ├─ 헤더: 로고(ITDA) + status-pill(dot+짧은 라벨) + ☰   │
│  │    └─ ☰ 메뉴: 카메라/음성/새창/모델 통합              │
│  ├─ LIVE TRANSLATION: 3.5s 보호 윈도우 (가시성 가드)     │
│  ├─ 통역 이력 패널: <body> fixed + localStorage          │
│  │    └─ 버퍼 누적("단어1 단어2") + 💬 문장 확정         │
│  └─ vision.js: WS broadcast 수신                         │
│                                                          │
│  avatar_only.html (새창)                                 │
│  └─ 학습 카드 / 수집 멀티-take / 단어 사이드바           │
└────────────────────────────────────────────────────────┘
                         │
                  WebSocket (8000)
                         │
┌─ Backend (port 8000) ─────────────────────────────────┐
│  api/main.py                                           │
│  ├─ knn_classifier : [복구] main + dialogue .pkl 양쪽  │
│  │     └─ /api/collect/train 으로 핫리로드 재학습      │
│  ├─ ws_vision      : 모든 활성 WS 에 broadcast (변경X) │
│  └─ slm_agent.build_sentence : 2초 idle 시 문장 합성   │
└────────────────────────────────────────────────────────┘
```

---

## 5. 🚨 향후 과제 (Remaining Tasks)

1. **인식 정확도 개선 — 데이터 다양성 확보**: 2026.05.22에서 확인된 핵심 병목(단어당 단일 수어자) 해소를 위한 멀티-take 다수자 수집 진행.
2. **모델 파일 백업 자동화**: 학습 완료 직후 `.pkl`을 타임스탬프 백업 디렉터리에 자동 복제하여 이번처럼 손실되었을 때 즉시 복구 가능하도록.
3. **인식 신뢰도 임계값 재조정**: dialogue 모델 CV 34%는 현재 `CONFIDENCE_THRESHOLD = 0.35`로는 통과율이 낮음. 사용자 피드백 기반으로 임계값과 모델 규모(80 → 핵심 단어 위주 축소)를 함께 조정.
4. **이력 패널 검색/필터**: 누적 100건 중 특정 단어·문장 빠른 탐색.
5. **시퀀스 모델 검토(이월)**: 데이터 확보 진척에 따라 LSTM 등 시퀀스 모델 도입 시점 평가.

---

## 6. 결론

2026.05.27 작업은 **운영 중 발생한 인식 불능 장애의 신속 복구**와, 그 과정에서 드러난 **UI 가시성·정보 구조 결함을 전면 정리**하는 데 집중되었습니다. KNN 모델은 핫리로드 재학습으로 복구되었고, 60fps 브로드캐스트로 인한 가시성 버그는 보호 윈도우로 해결되었으며, 메인 창은 잉여 컨트롤을 햄버거 메뉴로 통합하여 시각적으로 크게 정돈되었습니다. 통역 이력 패널은 localStorage 영속화·버퍼 누적·문장 확정 로직을 갖춰 백엔드 SentenceBuilder와 의미적으로 동기화된 표시 계층으로 재구성되었습니다.

이 작업들은 사용자 경험의 체감 품질을 큰 폭으로 끌어올렸으며, 향후 **데이터 다양성 확보**(2026.05.22의 최우선 과제)에 집중할 수 있는 깔끔한 운영 기반을 마련했습니다.

**작성자**: 개발팀
**통합 날짜**: 2026.05.27

---

## 🧭 문서 이력
- 2026.04.15: 프로젝트 초기 아키텍처 설계
- 2026.04.29: 3D 골격 렌더링 최적화 및 UX 개편
- 2026.05.08: 양손 3D 인식 엔진 및 자동 학습 파이프라인 구축
- 2026.05.12: 인식 엔진 정밀도 고도화(코사인 유사도), 아바타 가이드 시스템 적용
- 2026.05.13: KSL 데이터셋 파이프라인 자동화, 피처 엔지니어링 고도화
- 2026.05.14: 시계열 모션 특징(Velocity) 도입, Data Trimming, 서킷 브레이커 구현
- 2026.05.20: 웹캠 ON/OFF 제어, KNN 핫스왑, 크로스 탭 동기화, 대화 80단어 수어 데이터 완전 자동 빌드
- 2026.05.22: 전면 코드 품질 검토 및 Critical 버그 수정, 누수 없는 정직한 평가 체계, 출처 추적 및 멀티-take 수집 환경 구축
- 2026.05.27: KNN 모델 재학습 복구, 60fps 덮어쓰기 가시성 버그 수정, 메인 창 UI 전면 단순화(탭/카드/잉여 토글 제거 → 햄버거 메뉴 통합), 헤더 상태 표시 컴팩트화, 통역 이력 패널 영속화·버퍼 누적·문장 확정 구현
- **2026.05.29 (최신): Recognition Hero 카드(아바타 영역 큰 인식 디스플레이) 도입, LIVE TRANSLATION 카메라 오버레이 재설계, 텍스트→수어 모드 자동 전환(Hero ↔ 아바타), 모바일 전·후면 카메라 전환, 통역 이력 패널 정렬 개선, dialogue 모델 +40단어 추가(80 → 120라벨, 정확도 34.0 → 39.9%)**


---


# 📅 진행 날짜: 2026.05.29

# 🤝 Project ITDA(잇다): AI 기반 수어 번역 및 교육 플랫폼
## [개발 진행 보고서] — 2026.05.29 업데이트 (모드 인지형 UI 재설계 및 대화 모델 데이터 확장)

본 보고서는 2026.05.27 보고서의 UI 단순화·이력 영속화 성과를 바탕으로, **사용자 모드(수어→텍스트 vs 텍스트→수어)에 따라 자동 전환되는 인지형 UI 도입, 카메라 영역 비율 안정화 및 모바일 카메라 전환 지원, dialogue 모델 데이터셋 +40 단어 확장(120라벨로)** 작업을 정리한 문서입니다.

---

## 1. 프로젝트 개요 (Project Overview)
- **프로젝트 명**: ITDA (잇다)
- **핵심 목표**: 청각장애인과 비장애인의 원활한 소통을 위해 AI(SLM/RAG/Vision) 기술을 활용한 실시간 수어 번역 및 교육용 아바타 플랫폼 구축.
- **최상위 설계 원칙**: **"수화 캐릭터가 수화를 정확히 표현하고, 시스템이 사용자의 수어를 정확히 인식하는 것."** 단순한 동작 흉내를 넘어, 해부학적 정확도와 표준 수형 규격(KSL)을 준수하는 고정밀 양방향 통번역 환경 구현.

---

## 2. 전체 개발 진행률 (Overall Progress)

| 단계 | 개발 항목 | 진행 상태 | 비고 |
| :--- | :--- | :---: | :--- |
| **1-31단계** | UI 단순화·이력 영속화 통합 | 완료 | V7 통합 완료 |
| **32단계** | Recognition Hero 카드 도입 | **완료** | **[NEW]** 아바타 영역의 큰 인식 디스플레이 + 신뢰도 바 + 타임라인 |
| **33단계** | LIVE TRANSLATION 카메라 오버레이화 | **완료** | **[NEW]** 별도 영역 → 카메라 위 반투명 오버레이로 통합 |
| **34단계** | 모드 인지형 자동 전환 (수어↔텍스트) | **완료** | **[NEW]** 입력창 포커스·애니메이션 상태 추적하여 Hero/아바타 자동 토글 |
| **35단계** | 카메라 영역 비율 안정화 | **완료** | **[NEW]** opacity 기반 페이드로 토글마다 카메라 크기 출렁임 제거 |
| **36단계** | 모바일 전·후면 카메라 전환 | **완료** | **[NEW]** facingMode 토글 + 미러링 자동 보정 |
| **37단계** | 헤더 로고/상태 컴팩트화 v2 | **완료** | **[NEW]** SIGN 라벨 제거, status-pill(dot+짧은 라벨) 디자인 |
| **38단계** | 통역 이력 패널 정렬·앵커 개선 | **완료** | **[NEW]** 스켈레톤 뷰 상단·전체 폭 정렬, 영구 fixed 패널 |
| **39단계** | dialogue 모델 +40단어 확장 | **완료** | **[NEW]** sldict 배치 추출 + 동의어 충돌 사전 분석, 80 → 120라벨 |

---

## 3. 핵심 기술 개발 상세 (2026.05.29 업데이트)

### 3.1. Recognition Hero 카드 도입 (`frontend/index.html`)
사용자가 수어 인식을 사용 중일 때 텍스트가 너무 작게 표시되어 가독성이 떨어진다는 문제를 해결.
- **위치**: `#avatar-area` 중앙에 떠 있는 큰 카드 (`position: absolute; transform: translate(-50%, -50%)`)
- **구성 4단**:
  1. `실시간 인식 결과` 라벨 (시안, 작은 letter-spacing)
  2. **현재 단어** 대형 폰트(2.6rem) — 새 단어 도착 시 `hero-flash` 스케일+글로우 애니메이션
  3. **신뢰도 바**: 시안→보라 그라데이션 fill + 퍼센트 텍스트
  4. **최근 인식 타임라인**: 최대 5개 chip (문장은 보라, 단어는 시안)
- **접근성**: `aria-live="polite"`로 스크린 리더에서도 새 인식 알림 전달
- **자동 갱신**: `itda:rag:result` 이벤트 수신 시 `_updateHero(text, conf)` 호출로 4영역 동시 갱신

### 3.2. LIVE TRANSLATION 카메라 오버레이화 (`frontend/index.html`)
기존에 카메라 아래 별도 영역으로 존재하던 LIVE TRANSLATION 바를 **카메라 위 반투명 오버레이**로 재설계.
- **이전**: `#prediction-area`가 `#top-section`의 flex 자식으로 자체 높이(44~88px) 차지 → 카메라 영역 압박
- **이후**: `position: absolute; left:0; right:0; bottom:0` → flex 흐름에서 빠져나가 카메라가 `#top-section` 전체 차지
- **시각 디자인**:
  - `linear-gradient(180deg, transparent 0%, rgba(15,23,42,0.85) 55%, rgba(15,23,42,0.95) 100%)` — 위에서 아래로 자연스러운 페이드
  - `backdrop-filter: blur(6px)` + `text-shadow: 0 1px 3px rgba(0,0,0,0.6)` — 어떤 배경에서도 가독성 보장
- **라벨 제거**: 기존 `LIVE TRANSLATION` 라벨 텍스트는 Hero 카드가 동일 역할을 하므로 중복 제거

### 3.3. 모드 인지형 자동 전환 — Hero ↔ 스켈레톤 아바타 (`frontend/index.html`)
Hero 카드가 항상 떠 있으면 사용자가 텍스트→수어(아바타 동작 보기)를 할 때 아바타가 가려지는 문제.
- **상태 변수 3개** 도입:
  - `_cameraOn` — 카메라 ON/OFF
  - `_animatingT2S` — 텍스트→수어 애니메이션 중인지 (`btn-send` 클릭/Enter → true, `itda:motion:played` → false)
  - `_isTyping()` — 입력창 포커스 또는 비어있지 않으면 true
- **Hero 가시성** = `_cameraOn && !_isTyping() && !_animatingT2S` 의 AND
- **트랜지션**: `prediction-area`도 `opacity` 토글(0.25s 페이드)로 부드럽게 전환, `pointer-events`도 동기화
- **안전망**: `itda:motion:played` 이벤트가 어떤 이유로 누락돼도 8초 후 강제 해제 → Hero가 영원히 안 보이는 버그 방지

### 3.4. 카메라 영역 비율 안정화 (`frontend/index.html`)
초기 구현은 Hero 토글마다 `prediction-area`를 `display: none` ↔ `flex`로 전환해 카메라 비율이 출렁이는 시각적 결함이 있었음.
- **원인**: `display:none`이 형제 요소(camera-area)의 flex 비율을 변동시킴
- **해결 1차**: `visibility: hidden`으로 변경 → 공간은 유지되지만 빈 줄이 남아 있음
- **해결 최종**: `position: absolute` 오버레이화 + `opacity` 토글 → flex 흐름에 영향 없음. 어떤 토글에서도 카메라 크기 고정.

### 3.5. 모바일 전·후면 카메라 전환 (`frontend/js/vision.js`, `frontend/index.html`)
모바일 사용자가 후면 카메라로 다른 사람의 수어를 인식하거나 전면으로 자신의 동작을 확인할 수 있도록 지원.
- **`currentFacingMode`** 상태 변수: `'user'`(전면) ↔ `'environment'`(후면)
- **`startCamera()` 수정**: `getUserMedia` 호출 시 `facingMode: currentFacingMode` 사용
- **`switchCamera()` 신규 함수**:
  - 현재 스트림 정지 → 반대편 facingMode로 재시작
  - **미러링 자동 보정**: 전면이면 `scaleX(-1)`(자기 모습 반전), 후면이면 `scaleX(1)`(실세계 그대로)
  - 후면 카메라가 없는 디바이스는 자동 폴백 + "다른 카메라가 없습니다" 안내
- **UI**: `#cam-status-overlay`(손 개수/표정 칩 영역)에 `🔄 전면 ↔ 🔄 후면` 토글 버튼 추가, 라벨 자동 갱신

### 3.6. 헤더 로고·상태 표시 컴팩트화 v2 (`frontend/index.html`, `frontend/js/avatar.js`)
- **로고**: `ITDA SIGN` → **`ITDA`** (SIGN 텍스트 제거, 손 아이콘만 유지)
- **상태 pill** (3개): 박스형 hud-item → 둥근 dot+짧은 라벨 형식

| 상태 | 이전 | 이후 |
|---|---|---|
| 백엔드 | `Backend: OFFLINE` | `● OFFLINE` (red) |
| FPS | `FPS: 10` | `● 10 fps` (≥24 green / ≥12 yellow / <12 red) |
| 3D 모델 | `Joints: ACTIVE` / `3D 모델 비활성화` | `● 3D OFF` (yellow) |

- 전체 메시지는 `title` 속성으로 이동 → hover 시 툴팁
- `avatar.js`의 `jstEl` 갱신도 status-pill 포맷으로 통일

### 3.7. 통역 이력 패널 정렬·앵커 개선 (`frontend/index.html`)
이력 보기 패널의 위치 기준을 변경하여 더 큰 영역에 안정적으로 정렬되도록 조정.
- **버튼 이동**: 카메라 우상단 → 카메라 우하단 → 최종 **스켈레톤 뷰(`#avatar-area`) 우상단**
- **패널 정렬**: `_positionLogPanel()` 의 anchor 를 `prediction-area` → `camera-area` → 최종 `avatar-area`로 변경
- **결과**: 패널이 스켈레톤 뷰 상단·전체 폭에 깔끔하게 정렬, 토글 버튼이 가려져도 패널 내 ✖ 버튼으로 닫기 가능
- `z-index: 30` (이력 버튼) — Hero 카드(10)보다 위에 있어 항상 클릭 가능

### 3.8. dialogue 모델 +40단어 데이터 확장 (`scratch/check_words_for_user.py`, `scratch/add_new_dialogue_words.py`)
대화 모델의 어휘를 늘리고 추가 단어 도입 시 발생하는 데이터 충돌을 사전 분석하는 워크플로를 정립.

#### (a) 사전 충돌 분석 (`check_words_for_user.py`)
사용자가 추가하려는 50단어를 3축으로 분석:
- **라벨 직접 중복**: 기존 80단어와 동일 라벨이면 학습 제외 → `걱정`, `만나다` (2건)
- **영상(origin_no) 충돌**: 다른 라벨이지만 같은 sldict 영상을 가리키면 KNN 혼동 위험 → `가을`, `바람이 불다` (기존 '바람'과 origin_no=8328 공유)
- **내부 충돌**: 신규 50단어 내부에서도 같은 영상을 가리키는 동의어 그룹 5건 발견:

| origin_no | 동의어 그룹 |
|---|---|
| 10493 | 견디다 / 자제 |
| 8693 | 그저 / 여전하다 |
| 11571 | 다급하다 / 바쁘다 / 조급하다 |
| 11374 | 부랴부랴 / 허겁지겁 |
| 11501 | 부지런하다 / 열심 |

#### (b) 충돌 해소 — 콤마 묶음 단일 라벨
각 그룹을 하나의 콤마 묶음 라벨로 통합하여 같은 동작을 단일 클래스로 학습:
- `견디다,자제`, `그저,여전하다`, `다급하다,바쁘다,조급하다`, `부랴부랴,허겁지겁`, `부지런하다,열심`
- sldict 키 포맷과 동일한 컨벤션으로 통일

#### (c) 자동 배치 추출 (`add_new_dialogue_words.py`)
- 백엔드 활성 모델을 `dialogue`로 전환 (`/api/collect/select-model`)
- 40개 라벨 각각에 대해 `/api/collect/url` 호출 → 영상 다운로드 + MediaPipe 랜드마크 추출 + CSV 누적
- 마지막에 `/api/collect/train` 호출 → 모델 재학습 + 핫리로드
- **결과**: 40/40 성공, 단어당 100개 샘플 = 신규 **4,000행** 추가

#### (d) 모델 성장 지표
| 지표 | 이전 (2026.05.27) | 이후 (2026.05.29) | 변화 |
|---|---:|---:|---:|
| 라벨 수 | 80 | **120** | +40 (+50%) |
| 총 샘플 | 11,142 | **20,891** | +9,749 (거의 2배) |
| CV 정확도 | 34.0% | **39.9%** | **+5.9%p** |

라벨 수가 50% 늘었는데도 정확도가 오른 점이 유의미한 데이터 다양성 개선 신호.

---

## 4. 시스템 아키텍처 (2026.05.29 갱신)

```text
┌─ Frontend (port 3000) ─────────────────────────────────┐
│  index.html                                              │
│  ├─ 헤더: 로고(ITDA) + status-pill(dot+짧은 라벨) + ☰   │
│  ├─ 카메라: facingMode 토글(전면/후면) + 자동 미러링      │
│  │    └─ LIVE TRANSLATION: 카메라 위 반투명 오버레이     │
│  ├─ 아바타 영역:                                         │
│  │    ├─ Recognition Hero 카드 (큰 단어+conf+타임라인)   │
│  │    └─ 이력 보기 버튼(우상단) + 패널(상단 전체폭 fixed) │
│  ├─ 모드 인지형 자동 전환:                                │
│  │    Hero = cameraOn && !typing && !animating          │
│  └─ vision.js: switchCamera 공개 API + facingMode 추적   │
└────────────────────────────────────────────────────────┘
                         │
                  WebSocket (8000)
                         │
┌─ Backend (port 8000) ─────────────────────────────────┐
│  api/main.py                                           │
│  ├─ knn_classifier : dialogue 모델 120라벨 (재학습)     │
│  ├─ ws_vision      : 모든 활성 WS 에 broadcast          │
│  └─ slm_agent.build_sentence : 2초 idle 시 문장 합성   │
└────────────────────────────────────────────────────────┘

scratch/
  ├─ check_words_for_user.py  : 신규 단어 충돌 사전 분석
  └─ add_new_dialogue_words.py: sldict 배치 추출 + 재학습
```

---

## 5. 🚨 향후 과제 (Remaining Tasks)

1. **인식 정확도 추가 개선 — 멀티 수어자 데이터 확보**: dialogue 39.9%는 단일 수어자 데이터의 본질적 한계. 사용자 멀티-take 수집을 다수자로 확장하는 것이 최우선.
2. **콤마 묶음 라벨 표시 단순화**: `다급하다,바쁘다,조급하다` 같은 라벨이 UI에 길게 나오는 문제. 첫 단어만 강조 표시하고 나머지는 hover/tooltip 또는 작은 부제목으로 분리하는 UX 개선.
3. **이력 패널 검색·필터**: 누적 100건 중 특정 단어·시간대 빠른 탐색.
4. **시퀀스 모델 검토(이월)**: 데이터 확보 진척에 따라 LSTM 등 시퀀스 모델 도입 시점 평가.
5. **모바일 후면 카메라 인식 성능 검증**: 후면 카메라 환경에서의 MediaPipe 추적 안정성과 KNN 인식률 측정.

---

## 6. 결론

2026.05.29 작업은 **사용자의 모드(수어→텍스트 vs 텍스트→수어)를 시스템이 자동 감지하여 UI 가 따라가는 인지형 디자인**과 **모바일 환경 대응(전·후면 카메라 전환)**, 그리고 **대화 모델의 어휘 폭 50% 확장**으로 요약됩니다. 카메라 영역 비율 출렁임 같은 미세한 시각 결함부터, 신규 단어 도입 시 발생하는 데이터 충돌을 사전 분석하는 엔지니어링 워크플로까지 폭넓은 영역에 손이 들어갔습니다.

특히 dialogue 모델의 80 → 120라벨 확장이 정확도를 떨어뜨리지 않고 오히려 5.9%p 개선한 점은, 데이터의 양적 확보가 모델 성능에 직접 기여한다는 가설을 실측으로 확인한 의미 있는 성과입니다. 향후 멀티 수어자 데이터 확보 단계에서 더 큰 폭의 정확도 향상을 기대할 수 있는 토대가 마련되었습니다.

**작성자**: 개발팀
**통합 날짜**: 2026.05.29

---

## 🧭 문서 이력
- 2026.04.15: 프로젝트 초기 아키텍처 설계
- 2026.04.29: 3D 골격 렌더링 최적화 및 UX 개편
- 2026.05.08: 양손 3D 인식 엔진 및 자동 학습 파이프라인 구축
- 2026.05.12: 인식 엔진 정밀도 고도화(코사인 유사도), 아바타 가이드 시스템 적용
- 2026.05.13: KSL 데이터셋 파이프라인 자동화, 피처 엔지니어링 고도화
- 2026.05.14: 시계열 모션 특징(Velocity) 도입, Data Trimming, 서킷 브레이커 구현
- 2026.05.20: 웹캠 ON/OFF 제어, KNN 핫스왑, 크로스 탭 동기화, 대화 80단어 수어 데이터 완전 자동 빌드
- 2026.05.22: 전면 코드 품질 검토 및 Critical 버그 수정, 누수 없는 정직한 평가 체계, 출처 추적 및 멀티-take 수집 환경 구축
- 2026.05.27: KNN 모델 재학습 복구, 60fps 덮어쓰기 가시성 버그 수정, 메인 창 UI 전면 단순화(탭/카드/잉여 토글 제거 → 햄버거 메뉴 통합), 헤더 상태 표시 컴팩트화, 통역 이력 패널 영속화·버퍼 누적·문장 확정 구현
- 2026.05.29: Recognition Hero 카드(아바타 영역 큰 인식 디스플레이) 도입, LIVE TRANSLATION 카메라 오버레이 재설계, 텍스트→수어 모드 자동 전환(Hero ↔ 아바타), 카메라 영역 비율 안정화, 모바일 전·후면 카메라 전환, 헤더 로고/상태 컴팩트화 v2, 통역 이력 패널 정렬 개선, dialogue 모델 +40단어 추가(80 → 120라벨, 정확도 34.0 → 39.9%)
- **2026.06.02 (최신): 기본 모델(16단어/main) 폐기 및 dialogue 단일화, 학습 단어 목록 UX 고도화(검색·🆕새 단어 섹션·탭 UI), dialogue 데이터셋 정합성 작업(공백 통합·71 PENDING 어휘 등록·KSL 동의어 28개 통합), kor 데이터 병합·분포 불일치 진단 및 clip 임시 처리, 데이터 디렉터리 청소(~155MB 정리)**


---


# 📅 진행 날짜: 2026.06.02

# 🤝 Project ITDA(잇다): AI 기반 수어 번역 및 교육 플랫폼
## [개발 진행 보고서] — 2026.06.02 업데이트 (대화 모델 단일화, 학습 단어 UX 고도화, 데이터셋 정합성 작업)

본 보고서는 2026.05.29 보고서의 Hero 카드·인지형 UI·대화 모델 확장 성과를 바탕으로, **사용 안 하는 기본 모델(16단어) 제거 및 dialogue 단일 모델 체계 확립, 학습된 단어 목록 사이드바의 검색·새 단어 강조·탭 UI 도입, 사용자 어휘 71개 PENDING 등록 후 KSL 동의어 28개 통합, 외부 kor 데이터 병합 시 발생한 feature 분포 불일치 진단, 데이터 디렉터리 청소** 작업을 정리한 문서입니다.

---

## 1. 프로젝트 개요 (Project Overview)
- **프로젝트 명**: ITDA (잇다)
- **핵심 목표**: 청각장애인과 비장애인의 원활한 소통을 위해 AI(SLM/RAG/Vision) 기술을 활용한 실시간 수어 번역 및 교육용 아바타 플랫폼 구축.
- **최상위 설계 원칙**: **"수화 캐릭터가 수화를 정확히 표현하고, 시스템이 사용자의 수어를 정확히 인식하는 것."** 단순한 동작 흉내를 넘어, 해부학적 정확도와 표준 수형 규격(KSL)을 준수하는 고정밀 양방향 통번역 환경 구현.

---

## 2. 전체 개발 진행률 (Overall Progress)

| 단계 | 개발 항목 | 진행 상태 | 비고 |
| :--- | :--- | :---: | :--- |
| **1-39단계** | 인지형 UI 및 대화 120단어 통합 | 완료 | V7 통합 완료 |
| **40단계** | 기본 모델(16단어) 폐기 및 dialogue 단일화 | **완료** | **[NEW]** UI 옵션 제거 + 자동 폴백 + 데이터셋·main pkl 정리 |
| **41단계** | 학습 단어 검색 기능 도입 | **완료** | **[NEW]** 사이드바 🔍 입력 + 부분일치 강조 + Enter 순환 이동 |
| **42단계** | 🆕 새로 추가된 단어 섹션 | **완료** | **[NEW]** localStorage seen-words diff + 훈련 완료 broadcast 동기화 |
| **43단계** | 학습 단어 목록 탭 UI 전환 | **완료** | **[NEW]** 새 단어 / 기존 단어 패널 분리 + 탭별 검색 스코프 |
| **44단계** | dialogue 데이터셋 공백·중복 라벨 통합 | **완료** | **[NEW]** `견디다, 자제` 등 2쌍 공백 통합, 122 → 120 라벨 |
| **45단계** | 사용자 어휘 71개 PENDING 등록 | **완료** | **[NEW]** placeholder 행 + 훈련 시 자동 제외 + 마크다운 수집 예정 목록 |
| **46단계** | KSL 동의어 1차 정리 (16개) | **완료** | **[NEW]** 안전 동의어 제거 (예: 연인→애인, 테이블→탁자) |
| **47단계** | KSL 동의어 2차 통합 (28개, 사용자 도메인 지식 기반) | **완료** | **[NEW]** 기존 라벨에 콤마 묶음 동의어 추가, 글피/돌연 REAL→기존 라벨 흡수 |
| **48단계** | 외부 kor 데이터(200행) 병합 및 분포 불일치 진단 | **완료** | **[NEW]** legacy_kor source 마커 + ±5 범위 → ±1 clip 임시 처리 |
| **49단계** | 데이터 디렉터리 정리 | **완료** | **[NEW]** 5개 .bak + kor.csv + ksl_dataset.csv 삭제 (~155MB) |

---

## 3. 핵심 기술 개발 상세 (2026.06.02 업데이트)

### 3.1. 기본 모델(16단어/main) 폐기 및 dialogue 단일화 (`frontend/index.html`)
운영 중 main 모델(16단어 KSL 어휘)이 사실상 사용되지 않고, dialogue 모델(120라벨)이 모든 인식을 담당하고 있어 UI/데이터를 정리.

- **드롭다운 옵션 제거**: 헤더 ☰ 메뉴의 `인식 모델` `<select>`에서 `🏷️ 기본 모델 (16단어)` 옵션 삭제 → `💬 대화 모델 (80단어)` 단일 옵션만 노출
- **alert 문구 정리**: 모델 전환 시 표시되는 안내를 dialogue 단일 모델 기준으로 단순화
- **자동 폴백 로직**: 페이지 로드 시 백엔드가 `main` 상태로 부팅되더라도 즉시 `dialogue`로 자동 전환:
  ```js
  if (data.model_type === 'main') {
    await fetch('/api/collect/select-model', { ..., body: '{"model_type":"dialogue"}' });
    new BroadcastChannel('itda_model_channel').postMessage({ type: 'model_changed', model_type: 'dialogue' });
  }
  ```
- **데이터셋 정리**: `ksl_dataset.csv`(1.3MB, main 학습 데이터)와 `knn_model.pkl` 부재 상태를 확인하고 CSV도 삭제. 백엔드 코드에서 main 분기는 잔존하나 런타임 영향 없음(safe defaults).
- **결과**: 사용자 혼동 요소(쓰지 않는 옵션) 제거, dialogue 단일 모델 체계로 유지보수 단순화.

### 3.2. 학습 단어 검색 기능 도입 (`frontend/avatar_only.html`)
175개로 확장된 학습 단어 목록을 사용자가 빠르게 탐색할 수 있도록 사이드바에 검색 입력을 추가.

- **UI 구성**:
  - 사이드바 헤더에 🔍 아이콘 + 텍스트 입력 + ✕ 지우기 버튼 + 메타 상태 텍스트
  - 입력 포커스 시 시안색 보더 + glow
- **동작**:
  | 입력 | 동작 |
  |---|---|
  | 타이핑 (80ms 디바운스) | 부분 일치 단어 강조 + 첫 매치로 자동 스크롤 |
  | Enter | 다음 매치로 순환 이동 (`scrollIntoView({block:'center'})`) |
  | Esc | 검색어 지우고 포커스 해제 |
  | ✕ | 검색어 지우고 입력란 포커스 |
- **검색 강조**: 일치 단어는 `<mark>` 부분 강조 + 시안 외곽선/배경 + `search-flash` glow 애니메이션
- **재생 중 항목 보호**: `.playing` 클래스인 항목은 검색 강조 대상에서 제외(텍스트 충돌 방지)
- **`data-word` 캐노니컬 보관**: 각 단어 아이템에 `dataset.word`로 원본 텍스트 저장 → `<mark>` 강조 적용 후에도 매칭/복구 안전
- **`loadWordList` 재호출 후 자동 재적용**: 모델 갱신·훈련 완료 시 검색 상태 유지

### 3.3. 🆕 새로 추가된 단어 섹션 (`frontend/avatar_only.html`, `frontend/js/collect_ui.js`)
사용자가 새로 훈련시킨 단어를 즉시 인지할 수 있도록 학습 단어 목록 상단에 별도 강조 섹션을 추가.

#### (a) seen-words diff 알고리즘
- `localStorage` 키 `itda:avatar:seen_words:<model_type>`에 "이미 본 단어" Set 저장
- `loadWordList()` 호출 시:
  1. `/api/sign-language/list-motions`로 전체 단어 가져오기
  2. seen Set과 diff → 신규 단어 산출
  3. 신규 단어를 `🆕 새로 추가된 단어` 섹션에 NEW 배지와 함께 렌더
- **최초 진입 시**: 모든 단어를 seen으로 기록 → 빈 NEW 섹션 표시 (이후 추가되는 단어만 NEW로 인지)
- **모두 확인 버튼**: 현재 전체 목록을 seen으로 갱신 → NEW 섹션 비움

#### (b) 자동 갱신 채널 (`BroadcastChannel`)
- `itda_model_channel`에 `words_updated` 메시지 타입 신설
- `collect_ui.js`의 KNN 훈련 완료 직후 송신:
  ```js
  const ch = new BroadcastChannel('itda_model_channel');
  ch.postMessage({ type: 'words_updated', label_count: data.label_count });
  ```
- 아바타 뷰 수신 → `loadWordList()` 즉시 호출 → 새 라벨 자동 표시
- 백업: 20초 주기 폴링으로 broadcast 누락에 대비

#### (c) 시각 디자인
- 시안→보라 그라데이션 카드, `NEW` 배지, 카운트 뱃지(시안/회색 활성 상태 구분)
- 새 항목 등장 시 `section-in` slide-in 애니메이션

### 3.4. 학습 단어 목록 탭 UI 전환 (`frontend/avatar_only.html`)
검색·NEW 섹션·전체 목록이 한 화면에 동시 노출되면 정보 밀도가 높아 사용성이 떨어지는 문제를 탭으로 해결.

- **탭 바**: 사이드바 헤더 아래에 세그먼트 컨트롤 스타일의 `🆕 새 단어 / 📚 기존 단어` 두 버튼
- **패널 분리**:
  - 새 단어 패널: NEW 단어들만 표시 + "모두 확인" 버튼
  - 기존 단어 패널: 전체 목록에서 **NEW를 제외**하여 명확한 분리 (예: 175 − 55 = 120개)
- **활성 탭 표시**: 시안 배경 + 보더 + 카운트 뱃지 색상 전환(활성=시안/검정, 비활성=회색)
- **초기 탭 선택 우선순위**:
  1. localStorage(`itda:avatar:active_tab`) 마지막 선택값
  2. 신규 단어가 있으면 → `🆕 새 단어`
  3. 신규 단어 없으면 → `📚 기존 단어`
- **수동 전환 시 localStorage 영속화**: 다음 방문에도 마지막 선택 유지
- **"모두 확인" 클릭 후 자동 전환**: NEW 단어가 0이 되면 자동으로 `📚 기존 단어`로 이동
- **검색 스코프**: 활성 탭 내부 항목만 매치/스크롤 대상. 비활성 탭에 매치가 있으면 메타에 `(다른 탭 +N개)` 또는 `(🆕 새 단어 탭에 N개 있음)`으로 힌트
- **CSS 우선순위 충돌 해결**: `.word-panel:not(.active) { display: none !important }`로 각 패널의 기본 display 값 보존(.word-list는 flex 유지)

### 3.5. dialogue 데이터셋 공백 차이 라벨 통합 (`api/data/ksl_training/ksl_dataset_dialogue.csv`)
같은 KSL 수화이지만 콤마 뒤 공백 유무로 분리된 라벨 2쌍을 통합.

| 통합 전 | 샘플 수 | 통합 후 | 최종 샘플 수 |
|---|---|---|---|
| `견디다, 자제` + `견디다,자제` | 173 + 100 | `견디다,자제` | 273 |
| `그저, 여전하다` + `그저,여전하다` | 167 + 100 | `그저,여전하다` | 267 |

- **표준화 방향**: 데이터셋의 다른 다의 라벨(`다급하다,바쁘다,조급하다` 등)이 공백 없는 형식이므로 이에 맞춤
- **결과**: 122 → **120 unique 라벨**, 340행이 새 라벨로 이동
- 의도적 다의어(`다급하다,바쁘다,조급하다`, `부랴부랴,허겁지겁`, `부지런하다,열심`)는 보존

### 3.6. 사용자 어휘 71개 PENDING 등록 (`api/data/ksl_training/`, `api/routers/collect.py`, `ksl_word_categories.md`)
사용자가 제안한 105개 신규 어휘 후보 → 데이터셋 기존 라벨과 다의어 atom까지 비교해 중복 제거 → **71개 신규 단어** 도출.

#### (a) 분석
- 직접 중복(기존 라벨과 정확히 일치): 34개 (애인, 걱정, 소식, 냄새, 열매, 바람 등)
- 목록 내부 중복: 0개
- **신규**: 71개

#### (b) placeholder 행 방식 도입
KNN 모델은 라벨만으로 학습 불가(특성 샘플 필요)하므로 사용자에게 의사를 확인 → **CSV에 placeholder 행 추가** 선택:
- 71개 단어마다 1행씩 추가 (`f0~f75 = 0.0`, `source = 'PENDING'`, `label = 단어`)
- `list-motions` API가 unique label을 반환하므로 학습 단어 목록에 즉시 노출
- `🆕 새로 추가된 단어` 섹션이 자동으로 71개를 강조 표시

#### (c) 훈련 시 자동 제외 로직 (`api/routers/collect.py`)
PENDING 행이 KNN 학습 시 잘못된 클래스가 되지 않도록 자동 필터링:
```python
if 'source' in df.columns:
    pending_n = int((df['source'] == 'PENDING').sum())
    if pending_n:
        df = df[df['source'] != 'PENDING']
        print(f"[Collect] PENDING placeholder {pending_n}행 제외")
```

#### (d) 수집 예정 목록 정리
[ksl_word_categories.md](ksl_word_categories.md)에 `✨ 수집 예정 단어 — 대화 모델 (2026-06-02)` 섹션 신설. 의미 카테고리별로 정리:
- 명사(감정/관계/시간, 감각/자연/동물, 사물/생활, 사회/경제) / 형용사(가치/평가, 인내/집중, 변함없음, 다양함, 뛰어남) / 동사(지각/표현, 사라짐/변화, 동작/전달) / 시간 / 수사 / 기타 명사

### 3.7. KSL 동의어 정리 (1차 16개 + 2차 28개 = 총 44개)

#### 1차 — 안전 동의어 16개 자동 식별 (`scratch` 분석)
KSL 도메인 지식 기반으로 신규 71개 중 기존 라벨과 같은 수화일 가능성이 매우 높은 16개를 분류해 PENDING 행만 삭제:

| 신규 단어 | 같은 수화의 기존 라벨 |
|---|---|
| 연인 | 애인 |
| 만남 | 만나다 |
| 테이블 | 탁자 |
| 참다 | 견디다,자제 |
| 성급하다 | 다급하다,바쁘다,조급하다 |
| 한결같다 | 그저,여전하다 |
| 갖가지, 가지각색, 여러 가지 | 온갖 |
| 승리 | 이기다 |
| 가시다 | 사라지다 |
| 지시 | 시키다 |
| 부치다, 발송, 전송 | 보내다 |
| 귀중하다 | 중요 |

→ 71 → **55 PENDING**

#### 2차 — 사용자 도메인 지식 기반 28개 통합 (이후 사용자 직접 지정)
사용자가 직접 KSL 사인 동등성을 확인한 23개 단어에 대해 **기존 라벨에 콤마 묶음 동의어로 추가** + 일부 단어의 REAL 데이터를 합쳐 라벨 통합:

| 통합된 새 라벨 | 흡수된 단어들 | 총 행수 |
|---|---|---|
| `뜻밖에,돌연` | 돌연(REAL 55행) | 321 |
| `내일모레,글피,모레` | 글피(REAL 51행) + 모레(PENDING) | 301 |
| `보여주다,나타내다,드러내다,표시,표현` | 4개 PENDING | 303 |
| `들리다,듣다` | 듣다 | 307 |
| `쓰다,서술,필기` | 2개 | 297 |
| `온갖,형형색색` | 형형색색 | 321 |
| `늘어나다,증가` | 증가 | 305 |
| `보내다,주고받다` | 주고받다 | 371 |
| `부지런하다,열심,꾸준하다` | 꾸준하다 | 292 |

- **매칭 불명확 8개**(몫, 발표, 보람, 세월, 셋, 이틀, 지나다, 소리)는 PENDING placeholder만 삭제 (학습 단어 목록에서 제거)
- **결과**: 179 → **157 unique 라벨**, 22개 PENDING 행 삭제 + 2,712개 기존 라벨 행 재라벨, 글피/돌연 REAL 106행 흡수

### 3.8. 외부 kor 데이터(200행) 병합 및 분포 불일치 진단 (`api/data/ksl_training/ksl_dataset_kor.csv`)
외부에서 가져온 `ksl_dataset_kor.csv`(구기다 100 + 새롭다 100 = 200행)를 dialogue에 병합 후 카메라 인식 실패 → feature 분포 불일치 진단.

#### (a) 병합 작업
- kor CSV 구조: 77컬럼(f0~f75 + label), BOM 포함, source 컬럼 없음
- dialogue 형식에 맞춰 `source='legacy_kor'` 컬럼 삽입 후 200행 append
- 결과: 라벨 175 → 177 (구기다, 새롭다 추가)

#### (b) 인식 실패 원인 진단 — Domain Shift
재학습 후에도 구기다/새롭다 인식률 0%. 출처별 feature 통계로 비교:

| 출처 | 값 범위 (행 최댓/최솟) | non-zero feat/76 | 비고 |
|---|---|---|---|
| 🎥 LIVE (카메라) | **+1.0 ~ -1.0** (clip됨) | 38 | ml_utils 정규화 |
| 🌐 URL (영상) | +1.0 ~ -0.6 | 43 | 같은 파이프라인 |
| 📦 LEGACY (초기) | +1.0 ~ -0.6 | 40 | 같은 파이프라인 |
| ❗ **KOR (legacy_kor)** | **+5.07 ~ -3.73** | **62** | **정규화 안 됨, 다른 파이프라인** |

**핵심 문제**:
1. KOR 값 범위가 ±5까지 가는데 카메라는 ±1 clip → KNN 유클리드 거리에서 KOR 벡터는 카메라 벡터로부터 항상 멀리 떨어짐 → 절대 최근접 이웃이 될 수 없음
2. KOR의 non-zero feature 비율(62/76)이 카메라(38/76)보다 훨씬 높음 → 손 모양 표현 방식 자체가 다름

#### (c) clip [-1, 1] 임시 처리
정확도는 보장 불가하지만 일단 카메라 벡터와 거리 비교가 가능해지도록 KOR 행만 강제 clip:
- 클립된 행: 171/200, 총 클립된 값: 1,022개
- 클립 후 행 최댓값 평균: 1.117 → **1.000**, 행 최솟값 평균: -1.687 → **-0.957**
- **구조적 한계**: feature 추출 파이프라인 자체가 다르므로 clip만으로는 인식률 보장 불가. 정확한 해결은 원본 영상을 같은 파이프라인으로 재추출하는 것이 권장 사항으로 메모됨.

### 3.9. 데이터 디렉터리 정리 (`api/data/ksl_training/`)
실험·병합 과정에서 누적된 백업 파일들과 사용되지 않는 데이터셋 정리.

#### 삭제된 파일 (총 ~155MB)
- `ksl_dataset_dialogue.csv.bak` (28MB) — 최초 백업
- `ksl_dataset_dialogue.csv.pre_dup_remove.bak` (28MB)
- `ksl_dataset_dialogue.csv.pre_kor_merge.bak` (31MB)
- `ksl_dataset_dialogue.csv.pre_kor_clip.bak` (31MB)
- `ksl_dataset_dialogue.csv.pre_user_merge.bak` (36MB)
- `ksl_dataset_kor.csv` (252KB) — 이미 dialogue에 병합됨
- `ksl_dataset.csv` (1.3MB) — 폐기된 main 모델 데이터

#### 유지된 활성 파일
| 파일 | 크기 | 용도 |
|---|---|---|
| `ksl_dataset_dialogue.csv` | 36MB | 활성 학습 데이터 (157 라벨) |
| `knn_model_dialogue.pkl` | 23MB | 학습된 KNN 모델 |
| `retrain_global.py` / `combine_reports.py` / `generate_learned_motions.py` | 작음 | 유틸 스크립트 |

---

## 4. 시스템 아키텍처 (2026.06.02 갱신)

```text
┌─ Frontend (port 3000) ─────────────────────────────────┐
│  index.html                                              │
│  ├─ 헤더 ☰: 인식 모델 = dialogue 단일 옵션               │
│  │    └─ 자동 폴백: 'main' 상태 시 즉시 'dialogue' 전환  │
│  └─ (기존 Hero 카드 / status-pill / 이력 패널 유지)      │
│                                                          │
│  avatar_only.html (학습 단어 사이드바)                   │
│  ├─ 🔍 검색 (부분 일치 강조 + Enter 순환)                │
│  ├─ 탭 바: [🆕 새 단어 N] [📚 기존 단어 M]               │
│  │    └─ 활성 탭 localStorage 영속화                     │
│  ├─ 패널 1 — NEW words (seen-words diff)                 │
│  │    └─ NEW 배지 + "모두 확인" 버튼                     │
│  ├─ 패널 2 — 기존 단어 (전체 - NEW)                      │
│  └─ BroadcastChannel('itda_model_channel'):              │
│       'model_changed' / 'words_updated' 수신             │
└────────────────────────────────────────────────────────┘
                         │
                  WebSocket (8000)
                         │
┌─ Backend (port 8000) ─────────────────────────────────┐
│  api/main.py                                           │
│  ├─ knn_classifier : dialogue 단일 운용 (157 라벨)      │
│  ├─ collect.train_knn_model:                           │
│  │    └─ source=='PENDING' 행 자동 제외                 │
│  └─ sign_language.list-motions:                        │
│       └─ unique label 반환 (PENDING도 노출, 학습엔 무관)│
│                                                         │
│  api/data/ksl_training/                                │
│  ├─ ksl_dataset_dialogue.csv   (36MB, 157 라벨)         │
│  │    ├─ source='legacy', 'live_*', 'url:*' 등 정상    │
│  │    ├─ source='legacy_kor' (clipped ±1)              │
│  │    └─ source='PENDING' (수집 예정 placeholder)       │
│  └─ knn_model_dialogue.pkl     (23MB)                   │
└────────────────────────────────────────────────────────┘
```

---

## 5. 🚨 향후 과제 (Remaining Tasks)

1. **PENDING 33개 단어의 실제 데이터 수집**: 현재 학습 단어 목록에 노출되지만 KNN 모델은 인식 못 함. Collection Mode로 단어당 50~100 take 수집 → 재학습.
2. **kor 데이터(구기다/새롭다) 인식률 검증**: clip 임시 처리 후 카메라 인식 정확도 측정. 부족하면 원본 영상 재추출이나 라벨 제거 결정.
3. **이력 패널 검색·필터(이월)**: 누적 이력에서 단어/시간대 빠른 탐색.
4. **시퀀스 모델 검토(이월)**: 데이터 다양성이 확보되는 시점에 LSTM 등 도입 평가.
5. **main 모드 잔재 정리(선택)**: `api/brain/list_labels.py`, `collect.py:42`의 main 분기, `knn_classifier.py`의 `MODEL_PATH` 및 `set_model_type`의 'main' 허용 로직 제거.
6. **모바일 후면 카메라 인식 성능 검증(이월)**.

---

## 6. 결론

2026.06.02 작업은 **사용 안 하는 코드/데이터 정리**, **학습 단어 목록의 정보 구조 및 탐색성 개선**, **데이터셋의 정합성 강화**로 요약됩니다. 신기능보다는 운영 신뢰성과 사용성을 끌어올리는 작업이 중심이었으나, 학습 단어 사이드바에 도입한 검색·NEW 섹션·탭 UI는 향후 어휘가 더 늘어나도 사용자가 길을 잃지 않도록 하는 토대가 되었습니다.

특히 데이터셋 측면에서는 **공백 차이 같은 사소한 정합성 결함부터 외부 데이터 도입 시의 feature 분포 불일치까지 폭넓은 카테고리의 문제를 진단하고 처리**했으며, KSL 도메인 지식 기반의 동의어 통합으로 같은 의미를 표현하는 단어들이 단일 클래스로 학습되어 인식 모호성이 줄어들었습니다. 사용자 어휘 71개의 PENDING 등록 체계는 "수집 전 단어를 미리 시각화해 진척을 관리"하는 새로운 워크플로를 정착시켰습니다.

데이터 디렉터리는 ~155MB 정리되어 깔끔한 최소 구성으로 회복되었고, dialogue 단일 모델 체계는 이후 유지보수의 단순화와 운영 일관성에 기여할 것입니다.

**작성자**: 개발팀
**통합 날짜**: 2026.06.02

---

## 🧭 문서 이력
- 2026.04.15: 프로젝트 초기 아키텍처 설계
- 2026.04.29: 3D 골격 렌더링 최적화 및 UX 개편
- 2026.05.08: 양손 3D 인식 엔진 및 자동 학습 파이프라인 구축
- 2026.05.12: 인식 엔진 정밀도 고도화(코사인 유사도), 아바타 가이드 시스템 적용
- 2026.05.13: KSL 데이터셋 파이프라인 자동화, 피처 엔지니어링 고도화
- 2026.05.14: 시계열 모션 특징(Velocity) 도입, Data Trimming, 서킷 브레이커 구현
- 2026.05.20: 웹캠 ON/OFF 제어, KNN 핫스왑, 크로스 탭 동기화, 대화 80단어 수어 데이터 완전 자동 빌드
- 2026.05.22: 전면 코드 품질 검토 및 Critical 버그 수정, 누수 없는 정직한 평가 체계, 출처 추적 및 멀티-take 수집 환경 구축
- 2026.05.27: KNN 모델 재학습 복구, 60fps 덮어쓰기 가시성 버그 수정, 메인 창 UI 전면 단순화, 통역 이력 패널 영속화·버퍼 누적·문장 확정 구현
- 2026.05.29: Recognition Hero 카드 도입, LIVE TRANSLATION 카메라 오버레이 재설계, 모드 인지형 자동 전환, 모바일 전·후면 카메라 전환, dialogue 모델 +40단어 추가(80→120라벨, 34.0→39.9%)
- **2026.06.02 (최신): 기본 모델(16단어/main) 폐기 및 dialogue 단일화, 학습 단어 목록 UX 고도화(🔍 검색·🆕 새 단어 섹션·탭 UI), dialogue 데이터셋 정합성 작업(공백 통합·71 PENDING 어휘 등록·KSL 동의어 44개 통합), 외부 kor 데이터(구기다/새롭다 200행) 병합 및 feature 분포 불일치 진단·clip 임시 처리, 데이터 디렉터리 청소(~155MB 정리)**
