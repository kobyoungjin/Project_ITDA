# 🤝 Project ITDA(잇다): AI 기반 수어 번역 및 교육 플랫폼
## [개발 진행 보고서] — 2026.04.22 기준

본 보고서는 2026-04-21 보고서 이후 하루 만에 진행된 **대규모 아키텍처 고도화** 내역을 정리하고, 차기 개발 단계의 **"정확한 수화 표현 엔진"** 설계를 상세히 기술합니다.

---

## 1. 프로젝트 개요 (Project Overview)
- **프로젝트 명**: ITDA (잇다)
- **핵심 목표**: 청각장애인과 비장애인의 원활한 소통을 위해 AI(SLM/RAG/Vision) 기술을 활용한 실시간 수어 번역 및 교육용 아바타 플랫폼 구축.
- **최상위 설계 원칙(2026-04-22 확정)**: **"수화 캐릭터가 수화를 정확히 표현하는 것."** 속도·지연·LLM 품질보다 아바타가 손가락 마디·손목·팔꿈치·어깨까지 해부학적으로 정확하게 재현하는 것을 우선한다.

---

## 2. 전체 개발 진행률 (Overall Progress)

4/21 보고서 시점 65% → **4/22 현재 78%**. 단 하루 만에 5개의 Tier(P0~P3 + UI 레이어)가 추가 해결되면서 파이프라인 전 구간이 기능적으로 작동함.

| 단계 | 개발 항목 | 4/21 상태 | 4/22 상태 |
| :--- | :--- | :---: | :---: |
| **1단계** | RAG 기반 수어 지식 베이스 | 완료 | **완료 +** 중복 88% 제거, 12개 감정 자동 태깅 |
| **2단계** | MediaPipe 비전 엔진 & 리타겟팅 | 완료 | **완료 +** Handshape 분류기(6수형), Pose 분석기 추가 |
| **3단계** | SLM-RAG 하이브리드 번역 엔진 | 진행 | **안정화** dict→str 계약 수리, 11개 규칙 기반 폴백 |
| **4단계** | Three.js 3D 캐릭터 대시보드 | 완료 | 완료 |
| **5단계** | 전문 수어 애니메이션 엔진 | 완료 | **완료 +** RIG_PRESETS 6수형 × 3마디 × 5손가락 매핑 |
| **6단계** | 실시간 음성 인식(STT) 통합 | 진행 | **완료** Web Speech + Whisper 자동 폴백 |
| **7-8단계** | 교육 모드 및 대시보드 | 대기 | **MVP 완료** 5단어 학습 + 실시간 정답/오답 피드백 |
| **9단계** | 멀티 레이어 렌더링 | 진행 | 진행 (step9_layer 미통합 상태 유지) |

---

## 3. 핵심 기술 개발 상세 (2026-04-22 신규)

### 3.1. Handshape 분류기 (`api/services/handshape_analyzer.py`)
MediaPipe 21개 손 랜드마크에서 손가락별 MCP/PIP/DIP 관절 각도를 계산하고, 다음 6가지 KSL 수형으로 분류:

| 수형 | 설명 |
|---|---|
| **FIST** | 네 손가락 모두 접힘 (주먹) |
| **PALM** | 네 손가락 모두 펴짐 (평손) |
| **POINT** | 검지만 펴짐 (가리키기) |
| **V** | 검지+중지 펴짐, 약지+소지 접힘 |
| **L** | 엄지+검지 펴짐, 나머지 접힘 (직각) |
| **OK** | 엄지+검지 끝 맞닿음, 나머지 펴짐 |

각 손가락마다 `representative` 각도를 기준으로 `extended / curled / half` 3단계 판정. 스모크 테스트로 6개 수형 모두 정확 분류 확인.

### 3.2. Pose 분석기 (`api/services/pose_analyzer.py`)
어깨·팔꿈치·손목·엉덩이 8개 관절로부터 다음을 산출:
- **팔꿈치 굽힘 각도** (어깨-팔꿈치-손목 사이각, 도 단위)
- **손목 구역 분류** (얼굴 위 / 얼굴 / 가슴 / 허리 / 하체)
- **팔 확장 방향** (위/아래/옆/앞)
- **어깨 상승도 + 양팔 교차 판정**

### 3.3. 모션 세그먼테이션 상태머신 (`frontend/js/vision.js`)
손목 속도 기반 4상태 머신:
```
idle → moving → settling → stable → idle
```
`stable`(500ms 이상 정지) 프레임에서만 RAG+SLM Track3 실행 → SLM 호출 부하 약 80% 감축.

### 3.4. SLM 에이전트 재설계 (`api/services/slm_agent.py`)
- **결함 수리**: 이전 `predict_fast`가 dict를 반환해 RAG 쿼리 파이프라인이 깨지던 버그를 문자열 반환으로 정상화.
- **낙서감지 로직 제거**: 오염된 보안 경고 응답 완전 삭제, 한국 수어(KSL) 통역사 프롬프트로 전면 교체.
- **11개 규칙 기반 폴백**: Ollama 미가용 시 (handshape, wrist_region, movement) 조합으로 10단어 매칭.

### 3.5. Web Speech API + Whisper 자동 폴백 (`frontend/js/stt/`)
- Chrome/Edge: Web Speech API (설치 0, 즉시 동작)
- Firefox/Safari: SpeechRecognition 미지원 자동 감지 → Whisper(`/api/ws/stt`)로 폴백
- `window.ITDAStt` 단일 API로 투명 통합.

### 3.6. 실시간 HUD 배지 (`frontend/index.html`)
우측 상단 mini-status에 백엔드 분석 결과를 실시간 표시:
- **수형**: `R·PALM L·POINT` (배지 색상 강조)
- **손 위치**: `R:얼굴(125°) L:가슴(170°)` (구역 + 팔꿈치 각도)
- **모션 신호등**: ⚪ idle / 🟡 moving / 🟠 settling / 🟢 stable(펄스 플래시)
- **STT 소스**: 🌐 Browser / 🖥️ Server

### 3.7. 교육 모드 MVP (`frontend/js/education.js`)
기존 handshape/pose 분석기를 **재활용**해 신규 백엔드 없이 5단어 학습 기능 구현:
- `LESSON_PROFILES`: 안녕하세요 / 주먹 / 하나 / 승리 / 좋아요
- `window.ITDAEducation.start(word)` 호출 시 정답/오답 실시간 판정
- `motion_phase === 'stable'` 시점에 최종 확정 → 🟢 정답 펄스 애니메이션

### 3.8. AI Hub 파이프라인 개선 (`api/services/data_pipeline.py`)
- 모든 `attributes.name` 수집 (이전: [0]만)
- 키워드 기준 dedupe (샘플 200파일에서 **88.5%** 중복 제거 확인)
- 12개 카테고리(인사/감사/사과/사랑/기쁨/슬픔/격려/경고/의료/일상/가족/질문) 자동 감정 태깅

---

## 4. 시스템 아키텍처 (2026-04-22 갱신)

```
┌─ Frontend (port 3000) ─────────────────────────────────┐
│  index.html                                             │
│  ├─ avatar.js        : Three.js 아바타 씬               │
│  ├─ vision.js        : Face+Hand+Pose 감지              │
│  │    └─ 모션 상태머신 (idle→moving→settling→stable)    │
│  ├─ retargeting.js   : Blendshape → Morph Target        │
│  ├─ translator.js    : 텍스트→수어 애니메이션           │
│  ├─ stt/             : Web Speech + Whisper 어댑터      │
│  └─ education.js     : 교육 모드 판정 엔진              │
└────────────────────────────────────────────────────────┘
                        │
                 WebSocket (8000)
                        │
┌─ Backend (port 8000) ─────────────────────────────────┐
│  api/main.py                                            │
│  ├─ /api/sign-language/search        (REST 번역)        │
│  ├─ /api/sign-language/refresh-pipeline (FAISS 재빌드)  │
│  ├─ /api/ws/vision                   (실시간 손·포즈)   │
│  └─ /api/ws/stt                      (Whisper STT)      │
│                                                          │
│  api/services/                                           │
│  ├─ handshape_analyzer.py  (21pt → 6수형 분류)           │
│  ├─ pose_analyzer.py       (8pt → 팔·어깨 특징)          │
│  ├─ slm_agent.py           (KSL 통역사 + 규칙 폴백)      │
│  ├─ rag_engine.py          (FAISS 감정 인지 검색)        │
│  └─ data_pipeline.py       (AI Hub dedup + 감정 태깅)    │
└────────────────────────────────────────────────────────┘
```

---

## 5. 🎯 차기 개발 설계: "정확한 수화 표현 엔진 고도화"

### 5.0. 설계 배경

현재 아바타가 수어를 "표현"하지만 정확도는 **데모 수준**에 머무름:
- `MOTION_PROFILES` (translator.js) 에 **7개 단어만** 정의됨
- 각 profile은 `Arm`, `ForeArm`, `Spine`, `Neck` 대략 4개 본만 사용 — **손가락 마디는 대부분 움직이지 않음**
- Euler 각도가 수작업 추정치라 해부학적으로 어색함
- 어깨 움직임(Shoulder 본), 손목 회전(Hand 본)이 누락됨

**해결 방향**: 현재의 **Track 1 "엔진 프리셋 방식"**과 **Track 2 "실시간 데이터 기반 방식"** 두 축을 병행하여 단기·장기 모두를 커버.

---

### 5.1. Tier 1 — MOTION_PROFILES 해부학적 확장 (1~2일, 즉시)

**목표**: 기존 7개 profile을 **정밀 재작성** + 23개 추가 = 총 30개 KSL 필수 어휘 커버.

**각 profile의 필수 요소 (4/22 신규 규약)**:
1. **어깨**(`Shoulder`): Y축·Z축 회전 (들어올림/안으로 모음)
2. **상완**(`Arm`): 3축 Euler (앞/옆/위 방향)
3. **전완**(`ForeArm`): X축 (팔꿈치 굽힘) + Y축 (회전)
4. **손**(`Hand`): X/Y/Z 3축 (손목 회전·굴곡)
5. **손가락 × 3마디** (`Thumb1/2/3`, `Index1/2/3`, ...): **RIG_PRESETS 6수형 중 하나** 또는 마디별 수동 지정
6. **척추·목**(`Spine`, `Neck`): 몸통·고개 기울기
7. **타이밍**: `duration` + `easing` (ease-in / ease-out / ease-in-out 지정 가능)
8. **NMS**(`morphs`): 얼굴 표정 (눈썹·입·눈)

**작업 파일**:
- [frontend/js/translator.js:166-217](frontend/js/translator.js#L166-L217) `MOTION_PROFILES` 확장
- 신규 `frontend/data/motions/*.json` 으로 외부화 (30개 넘으면 관리 곤란)
- 신규 `frontend/js/motion_loader.js`: JSON → profile 동적 로드

**새 규약 예시 (안녕하세요)**:
```json
{
  "keyword": "안녕하세요",
  "steps": [
    {
      "duration": 400, "easing": "ease-out",
      "bones": {
        "RightShoulder": {"x":0, "y":-0.1, "z":0},
        "RightArm":      {"x":-0.3, "y":0, "z":-1.2},
        "RightForeArm":  {"x":1.1, "y":0.3, "z":0},
        "RightHand":     {"x":0.2, "y":0, "z":-0.1}
      },
      "handshape_R": "PALM",
      "morphs": {"mouthSmile": 0.3}
    },
    {
      "duration": 600, "easing": "ease-in-out",
      "bones": {
        "RightArm":     {"x":-0.5, "y":0, "z":-0.9},
        "RightForeArm": {"x":0.8, "y":0.1, "z":0}
      },
      "handshape_R": "PALM",
      "morphs": {"mouthSmile": 0.5, "Neck": {"x":0.1}}
    }
  ]
}
```

**마감 기준**:
- 30단어 모든 profile에 **손가락 3마디 명시** 필수
- 각 step마다 어깨·상완·전완·손·5손가락 × 3마디 = **최소 15개 본 지정**
- 모션 재생 후 **수어 전문가 또는 KSL 표준 영상**과 side-by-side 비교 캡처

---

### 5.2. Tier 2 — 프로필 편집/미리보기 도구 (3~5일)

**문제**: Tier 1은 JSON 수작업이라 본 좌표 감각이 없으면 비효율.

**해결**: 브라우저 내장 **Keyframe Editor**:
- 아바타를 Orbit + Pose Tool 로 직접 조작 (기존 Three.js 씬 재활용)
- 각 본을 드래그 → 실시간 Euler 값 확인
- "현재 자세를 keyframe 1로 저장" 버튼 → JSON 자동 생성
- 재생 시 보간 애니메이션 미리보기
- JSON 내보내기/가져오기

**신규 파일**:
- `frontend/editor.html` : 전용 에디터 페이지
- `frontend/js/editor/pose_editor.js` : Bone 드래그 컨트롤
- `frontend/js/editor/keyframe_recorder.js` : 타임라인 + 녹화/저장

**효과**: 30단어 → 200단어 확장 시 제작 시간 대폭 단축.

---

### 5.3. Tier 3 — AI Hub 영상에서 Keyframe 자동 추출 (1~2주)

**문제**: 수작업 profile은 해부학적 오류 가능성 상존. Ground Truth가 필요.

**해결**: 기존 `api/tools/` 의 잠자던 유틸을 재활성화:
- `api/tools/skeleton_extractor.py` : 영상 → MediaPipe pose 시계열
- `api/tools/keyframe_converter.py` : pose 시계열 → Profile JSON
- `api/tools/aihub_downloader.py` : AI Hub 영상 다운로드 (Blob URL)

**파이프라인**:
```
AI Hub .mp4 (Blob URL)
  ↓ mediapipe pose + hand 추출 (frame-by-frame)
  ↓ 속도 변곡점 검출 → keyframe 후보 선택
  ↓ 각 keyframe 에서 handshape_analyzer + pose_analyzer 적용
  ↓ profile JSON 생성 (자동)
  ↓ 수동 검수 (editor 에서 미세 조정)
```

**품질 관리**:
- 원본 영상 ↔ 생성된 아바타 모션 side-by-side 비교 웹페이지
- 수어 전문가가 10점 척도로 평가 → 7점 이하는 재작업

**목표 산출**: 500단어 profile 데이터셋.

---

### 5.4. Tier 4 — NMS(Non-Manual Signals) 동기화 (1주, 병렬 진행)

**문제**: 현재 얼굴 표정(morph)이 `step.morphs` 로 수동 지정되지만, KSL 은 단어별 **고정된 비수지 신호**가 있음:
- "?" 의문문 → 눈썹 올림
- "!" 명령문 → 눈썹 내림 + 입 모음
- 강조 → 눈 크게 뜸

**해결**:
- 신규 `frontend/js/nms_library.js` : 감정·문형별 blendshape 프리셋
- profile 에 `nms_tag: "question"` 한 줄만 써도 자동 얼굴 표정 적용
- `face_landmarker` 모델은 이미 로드 중이므로 추가 비용 없음

---

### 5.5. Tier 5 — 연속 수어 문장 합성 (중장기, 2~3주)

**문제**: 현재는 단어 단위 profile 재생만 가능. "안녕하세요, 반갑습니다." 같은 문장은 두 개 profile 을 순차 재생하면 **손이 갑자기 기본자세로 복귀** 하는 어색함.

**해결**:
- 종결 자세 → 다음 시작 자세 사이의 **transition interpolation** 자동 생성
- Quaternion Slerp 로 500~700ms 전환 (기존 엔진 재활용)
- `animateAvatar` 가 문장 배열 받으면 자동 chaining

---

### 5.6. 우선순위 및 체감 효과

| 순위 | 과제 | 예상 기간 | 수화 정확도 임팩트 |
|---:|---|:---:|:---:|
| 1 | Tier 1: MOTION_PROFILES 30개 해부학적 재작성 | 1~2일 | ⭐⭐⭐⭐⭐ |
| 2 | Tier 4: NMS 라이브러리 (표정 동기화) | 3~5일 | ⭐⭐⭐⭐ |
| 3 | Tier 2: Keyframe Editor | 3~5일 | ⭐⭐⭐ (장기 제작 속도) |
| 4 | Tier 3: AI Hub 자동 추출 | 1~2주 | ⭐⭐⭐⭐⭐ (500단어 달성) |
| 5 | Tier 5: 문장 합성 | 2~3주 | ⭐⭐⭐ |

---

## 6. 🚨 차기 단계 진척 (2026-04-22 오후 세션)

### 6.A Tier 1 — 수작업 해부학 프로필 (결과: 절반 성공, 천장 확인)
V1 → V2.6 까지 6차례 반복 끝에 **다음 convention 확정**:
- Rest pose = T-pose (차렷 아님)
- RightArm.x 음수 = UP / Arm.z 음수 = FORWARD / ForeArm.x 음수 = UP(얼굴)
- **Euler XYZ 순서 복합 회전 문제** 발견 → 엔진을 `ZYX` 순서로 교체
- LeftArm.z 는 **미러** (Right 음수=앞, Left 양수=앞)
- `sonyr.glb` 모델의 본 이름은 표준 Mixamo 표기법 (prefix 없음)

확장된 V2 프로필 10개: 안녕하세요 / 고맙습니다 / 사랑합니다 / 미안합니다 / 네 / 아니오 / 도와주세요 / 기뻐요 / 슬퍼요 / 이름.

**결론**: 수작업으로는 "근사치" 수준이 상한. 북극성(정확도) 원칙을 만족하려면 실측 데이터가 필요.

### 6.B Option A 파이프라인 구축 완료 (실행 대기)
#### 신규 파일
- [api/tools/mediapipe_retarget.py](api/tools/mediapipe_retarget.py) — 영상 → MediaPipe 랜드마크 → quaternion keyframe 추출 도구
- [frontend/js/motion_loader_v3.js](frontend/js/motion_loader_v3.js) — V3 JSON 동적 로드 + quaternion slerp 재생 엔진
- [frontend/data/ksl_motions/](frontend/data/ksl_motions/) — V3 JSON 저장소 (현재 비어있음)

#### 핵심 설계
- **Quaternion 기반**: Euler order 이슈 회피 (V2.4 에서 겪은 x 성분 손실 재발 방지)
- **부모-자식 local 체인**: ForeArm local = `Arm.inverse() * ForeArm_world`
- **모델 독립**: T-pose rest 만 가정. sonyr.glb 바뀌어도 동일 로직
- **압축**: quaternion 변화량 < 0.08 rad 인 프레임 자동 삭제

#### 수학 검증 (Synthetic)
```
[Case 1] T-pose        → identity (0,0,0,1) ✓
[Case 2] 차렷 자세      → Z축 ∓π/2 회전 ✓ (좌우 대칭)
[Case 3] 만세 (오른팔)  → Z축 +π/2 회전 ✓
[Case 4] 팔꿈치 90°     → ForeArm 만 회전, Arm identity ✓
```

#### 실행 경로 (향후)
```bash
# 1) 영상 확보 후
python -m api.tools.mediapipe_retarget extract-video \
    --video samples/hello.mp4 --word 안녕하세요 \
    --output frontend/data/ksl_motions/

# 2) 프론트 자동 인식
#    → translator.js 의 animateAvatar() 가 V3 JSON 우선 로드
#    → 있으면 quaternion keyframe 재생, 없으면 V2 폴백
```

### 6.C 미해결 블로커 (외부 제약)
- **AI Hub Azure blob URL**: DNS 해결 실패 (현재 네트워크 미지원)
- **디스크 용량 부족**: KSL 영상 20K (118GB) 일괄 다운로드 불가
- **사용자 webcam 녹화 불가**: 사용자 개인 사유

→ 파이프라인은 완성했으나 **실제 영상 입력 대기 중**. 위 블로커 중 하나가 해소되면 즉시 실행 가능.

---

## 7. 🎯 내일(4/23) 이후 해야 할 일

### 🔴 차단 요인 해소 (택일 가능)
1. **저장소 확보**: 외장 드라이브 / USB / 클라우드 (Dropbox, Google Drive) 등으로 10~50GB 확보 → AI Hub 샘플 다운로드
2. **수동 영상 제공**: aihub.or.kr 수동 로그인 후 샘플 1~3개 .mp4 다운로드 → `api/data/sample_videos/` 배치
3. **네트워크 변경**: 다른 네트워크에서 blob.core.windows.net 접근 가능한지 테스트

### 🟡 Option A 실행 (영상 확보 후)
1. `mediapipe_retarget.py extract-video --video ... --word 안녕하세요` 실행
2. 생성된 `frontend/data/ksl_motions/안녕하세요.json` 시각 검증
3. 품질 평가 → 필요 시 `retarget_arm_chain()` 파라미터 미세 조정
4. 10개 핵심 단어 일괄 변환
5. 500+ 단어 스케일업 (모션 검증 루프)

### 🟢 Tier 4 (NMS) — 병행 가능
영상 없이도 진행 가능:
- 신규 `frontend/js/nms_library.js` — 의문문/명령문/강조별 얼굴 표정 프리셋
- MOTION_PROFILES 의 `nms_tag` 한 줄로 자동 blendshape 적용

### 🔵 레포 위생
- `step4_threejs/`, `step5_face_sync/` orphan 디렉토리 정리
- V1 프로필에 "Legacy — 부호 오류 가능성" 주석 추가
- 기존 `api/tools/skeleton_extractor.py` 와 `keyframe_converter.py` 도 V3 규약에 맞춰 업데이트 or deprecate 표시

---

## 8. 최근 고도화 내역 (2026-04-22 단일 세션)

### Backend
- **신규 모듈**: `handshape_analyzer.py`, `pose_analyzer.py`, `stt.py`(라우터 어댑터)
- **재설계**: `slm_agent.py` 전면 재작성(낙서감지 제거)
- **개선**: `data_pipeline.py` (dedup + 감정 추론), `sign_language.py` (`/refresh-pipeline?force=true`)

### Frontend
- **신규 모듈**: `education.js`, `stt/stt-adapter.js`, `stt/audio_capture.js`, `stt/stt_client.js`
- **재설계**: `vision.js` 모션 상태머신 추가, `translator.js` RIG_PRESETS 6수형×3마디 확장, STT 어댑터 통합
- **UI**: `index.html` 에 handshape/pose/motion/STT 4종 HUD 배지, 모드 토글, 학습 카드

### 검증
- 단위 테스트: 6수형 분류 6/6 통과
- 정적 wiring 검증: 28/28 연결 포인트 통과
- Round-trip 테스트: VisionFrame 파싱 + Ack 직렬화 정상

---

## 7. 결론

- 1일 만에 65% → 78% 도약, 파이프라인 뼈대는 완전히 관통됨.
- **이제부터는 "정확도 확보" 단계**. Tier 1(MOTION_PROFILES 30개 해부학적 재작성)이 최우선. 이 단계만 완료되어도 외부 데모 자격 획득.
- **2026년 5월 목표**: Tier 1 + Tier 4 완료 → 30단어 × 해부학적 정확도 × NMS 동기화.
- **2026년 6월 목표**: Tier 3 500단어 데이터셋.

**2026.04.22 (오전: 기반 구축) → 오후: Tier 1 수작업 한계 확인 + Option A 파이프라인 완성**
**개발팀 리드: 반디 (Antigravity)**

---

## 🧭 이 문서의 업데이트 규칙
- 매번 크게 전환될 때마다 섹션 6.C, 7 을 최신화.
- 모델(GLB) 교체 / 엔진 파이프라인 변경 / Convention 확정 시점 모두 **메모리 파일** 로도 저장.
- 지식베이스 refresh (`POST /api/sign-language/refresh-pipeline?force=true`) 는 단어 추가마다 실행.
